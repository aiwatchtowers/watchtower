---
description: A skill both personas could use, switched off by its own frontmatter.
persona: both
enabled: false
unknown-key: ignored on purpose
---

# Valid disabled

Body. This fixture pins two things at once: `enabled: false` is honoured, and
an unknown frontmatter key is ignored rather than rejected.
