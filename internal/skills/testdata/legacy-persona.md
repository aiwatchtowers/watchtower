---
description: Carries a legacy persona key from the two-persona era, ignored like any unknown key.
persona: butler
---

# Legacy persona

Body. Listed by both parsers: `persona` is an unknown key today, so even a
value that was never valid must not make the file unlistable.
