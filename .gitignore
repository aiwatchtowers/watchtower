# Build output
watchtower
build/
WatchtowerDesktop/.build/
WatchtowerDesktop/*.app

# Xcode per-user state (WatchtowerMobile.xcodeproj is committed; user data is not)
xcuserdata/
*.xcuserstate
DerivedData/

# macOS
.DS_Store

# IDE
.idea/
*.iml

# Claude Code local settings
.claude/

# Task tracking files
TASK-*.md
.ralphex/

# Git worktrees
.worktrees/

# OMC / Ralph scratch files (PRD, progress log, session state)
.omc/

# Environment and secrets
# Local signing config (DEVELOPMENT_TEAM etc.) — see Signing.xcconfig.template
WatchtowerMobile/Signing.xcconfig
.env*
*.pem
*.key
*.p12
*.csr
cert

# Database files
*.db
*.sqlite

# Logs
*.log

# User config (contains tokens)
config.yaml

# Sentrux runtime artifacts (rules.toml is committed; baseline is local)
.sentrux/baseline.json

# Release artifacts
dist/

# App bundles (redundant safety)
*.app/
docs/strategy/*
# Claude Code client-side MCP auth cache (created in cwd, not project data)
mcp-needs-auth-cache.json

# Stray Claude Code session data (transcripts/memory) accidentally created in-repo
projects/-Users-user-PhpstormProjects-watchtower/
