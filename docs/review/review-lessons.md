# Review Lessons

Soft-calibration log for `debate-review`. The judge appends ONE dated block per run (Step 4
reflection); the operator later fills the `outcome:` line after the real human review / merge
(`debate-review --outcome <PR#>`). **Observations, not binding rules** — every review agent
reads this as calibration. When a lesson proves out, the operator promotes it into
`docs/review/review-rules.md` under the matching dimension and marks it `[promoted]` here.

Entry format:

```markdown
## <date> — <branch or PR#> — verdict: <approve|changes-needed>

- contested: <unresolved point the judge had to settle> [dimension]
- false-positive: <finding raised then dismissed, and why> [dimension]
- miss: <issue caught only by codex / late / by a human> [dimension]
- weak-dimension: <dimension that underperformed>
- rule-gap: <rule that was missing or that proved useful>
- outcome: <TBD until the --outcome step records the real result>
```

---

<!-- judge appends new lesson blocks below this line -->

## 2026-08-04 — fix/single-instance-guard (408351a2/75854a35, fast-mode round 3, FINAL) — verdict: approve

- contested: disposition of the last formally-open corner — prosecutor's brute force over all 96 pair observation combinations found 2 mutual-exit cases via the (nil,nil) pid tie-break, reachable only under pid-counter wrap between two simultaneous cold launches PLUS asymmetric observability. Settled: documented-accepted-limitation (doc comment qualified in 75854a35, judge-verified the wording states the pid-monotonicity assumption and drops the absolute claim), NOT a code change — the (nil,nil)→false alternative flips a deliberate R1 policy pin and guarantees a duplicate in the COMMON both-nil race to close an effectively-unreachable one. When no per-process total order over inconsistent views can close a corner, the correct terminal state is a stated assumption, not another ordering tweak. [6 AC]
- false-positive: R3-F2 (shared-array test shape can't pin the pid-wrap corner) — rejected as moot, concurring with the orchestrator: once the corner is an accepted documented limitation, a test pinning the non-guarantee asserts nothing the code promises. Distinguish "test can't catch accepted limitation" (fine) from "test can't catch claimed invariant" (R2's real weak-dimension-7 finding, which WAS fixed via per-side asymmetric pre-fix-failing tests). [7]
- miss: codex zero-findings for the third consecutive round on this branch (R1 echo, R2 echo, R3 nothing) — on small fast-mode Swift/shell diffs the codex lane consistently adds no unique depth here; the prosecutor's mechanical exhaustion (96-combination brute force, mutation-tested shell suite) carried every unique R2/R3 catch. Weight lanes accordingly on this diff shape. [process]
- weak-dimension: none this round — dimension 7 recovered fully (asymmetric per-side tests verified pre-fix-failing; ordering/END-marker/sibling-dir guard tests discriminating and fail-closed).
- rule-gap: R2 candidate (b) proved out verbatim — "a mutual-deference guard's failure DIRECTION under observation asymmetry is a policy choice that must be explicit in the doc comment" is exactly what closed the final finding; promote it. Companion observation: an exhaustive small-state-space enumeration (all observation pairs of a two-party ordering) is a cheap prosecutor move that found the only R3 finding — worth listing in the toolkit for consensus/tie-break code. [6/9]
- outcome: TBD

## 2026-08-04 — fix/single-instance-guard (01fc10b9, fast-mode round 2) — verdict: changes-needed

- contested: nil-observability policy (prosecutor F1 zero-survivor vs code-reviewer's "mismatch → never defer" proposal vs prosecutor F2's counter) — settled: the residual is REAL but no local deterministic total order can close an asymmetric-observation race (each side ranks over a DIFFERENT view; this is consensus without communication). Judge-verified the `(nil, .some) → true` arm never fires on the incident path (the runner there sees a dated long-running peer), so flipping to "observability mismatch → never defer" guarantees ≥1 survivor, fails toward the recoverable duplicate, and weakens nothing shipped — but it inverts `test_nilDatePeerAgainstDatedCurrent_peerWins`, a deliberate round-1 policy pin, so it is owner sign-off, not a fixer edit (the "strictly stronger assertions need no sign-off" carve-out does not cover a direction flip). Filed Needs-human with a recommendation, not a blocker: requires two near-simultaneous cold launches inside a sub-second observability window, outcome is a relaunch. [6 AC]
- contested: space-in-path fail-open severity — 5-lane existence convergence (prosecutor, codex, test-analyzer, silent-failure, code-reviewer) but every lane individually rated it minor/medium; elevated to MAJOR on the 2026-06-19 convergence precedent PLUS class: a silently-inoperative guard on a space-containing checkout path is invisible degradation of the branch's entire purpose (rebuild-under-live-process is a known Security.framework breaker), and the `index($0,p)==1` fix was already mechanically verified 16/16 green by a lane, preserving the argv-mention property by the no-leading-whitespace argument. Cheap verified fix + silent-degradation class + unanimous existence = elevate despite unanimous per-lane minor ratings. [9/6]
- contested: prosecutor F3 relative-path fail-open (`ps command=` reports argv[0] AS TYPED; `./build/watchtower daemon` never prefix-matches absolute $BUILD_DIR) — settled MINOR, documented-limitation disposition: the candidate real fixes are worse (bare `build/` prefix match false-positive-blocks on ANY repo's relative build/ process machine-wide; lsof is slow/permission-dependent), primary consumers (app bundle, make, daemon spawn) launch absolute, and fail-open equals the pre-branch status quo of a defense-in-depth layer. The empirical verification (argv-as-typed) was prosecutor-unique and high quality. [9]
- false-positive: advocate W3's "the space-path fix has portability costs" — disproven by code-reviewer actually running the $0 variant against all 16 tests; second consecutive round on this branch where an advocate defense resting on a checkable fact was wrong (round 1: "shell scripts untested by repo convention"). Also: test-analyzer #4 (trailing-slash sibling dir) is already impossible by construction — `p="$BUILD_DIR/"` includes the slash. [7]
- miss: codex contributed only an echo (space path, P2) — on a small fast-mode diff the unique depth again came from specialists and the prosecutor (relative-path empiricism, mutation-tested shell suite, brute-forced isOlder equivalence). [process]
- weak-dimension: 7, narrower than round 1 but same shape twice — the exactly-one-survivor tests feed one shared candidates array to both perspectives (structurally cannot fail on the asymmetric residual they were written against), and the guard-before-rm-rf ORDERING is unasserted (marker-extracted tests stay green if the block moves below the rm -rf). Fix-batch regression tests keep pinning the symmetric restatement of an asymmetric bug.
- rule-gap (candidates): (a) a ps/proc-table–matching guard must be pinned against the launch shapes users actually type (relative argv[0], paths with spaces) or document each excluded shape at the pattern site — "green over absolute-path fixtures only" is the tell, dim 7/9; (b) a mutual-deference guard's failure DIRECTION under observation asymmetry (zero survivors vs two) is a policy choice that must be explicit in the doc comment and pinned from BOTH perspectives of the same asymmetric pair, dim 6.
- outcome: TBD

## 2026-08-04 — fix/single-instance-guard (f4de88d9, fast-mode round 1) — verdict: changes-needed

- contested: F1 severity/remedy (prosecutor "notification payload delivered to no process" vs advocate "deliberate defense-in-depth trade-off") — settled MAJOR, 4-lane convergence (prosecutor, codex, code-reviewer conf-85, silent-failure MED5). Judge-verified in the diff: `terminateIfDuplicate()` is the first line of `WatchtowerApp.init`, `UNUserNotificationCenter.current().delegate` is installed two lines later — so on the runtime-guard path the click's response lands nowhere (pre-fix it at least executed, wrongly, in the duplicate). The plist `LSMultipleInstancesProhibited` layer preserves the payload only when LS honors it for the differently-registered-copy scenario, which is exactly the incident and is unproven. Not blocker: the change's own AC (no duplicate instance) is met and the running app is activated; minimum bar = document the trade-off + log the drop; payload forwarding is an owner scope call (Needs human). [6 AC]
- contested: F2 mutual-exit race — highest lane convergence of the review (prosecutor, advocate concession W1, test-analyzer Gap1, silent-failure HIGH2, code-reviewer = 5 lanes) yet capped MAJOR (test-analyzer crit-6 < 8 bar; requires two near-simultaneous cold launches). The composite fix shape (defer only to strictly-older by launchDate, tie-break lowest pid, skip `isTerminated`) closes Gap3's defer-to-dying-instance zero-survivor path in the same signature change. [6/9]
- false-positive: F7 (guard "not literally first" — stored-property initializers run before init body) — advocate verified `AppState()`/`NotificationDelegate()` construction is side-effect-free; demoted to a comment-only nit. [1]
- false-positive (advocate-side, load-bearing): the defense "shell scripts untested by repo convention" is factually wrong — `scripts/tests/test-build-app-signing.sh` exists with a BEGIN/END block-extraction harness, judge-verified. The wrong claim would have dismissed test-analyzer Gap2; a defense resting on a checkable repo fact must be grep-verified before it's allowed to settle a finding. [7]
- miss: nothing codex-unique this round (codex = F1 echo only; filed nothing on tests or build-app.sh). The unique depth came from specialists: silent-failure alone caught the guard-vs-blast-radius mismatch (`grep -F "$APP_BUNDLE/Contents/MacOS/"` vs `rm -rf "$BUILD_DIR"` also destroying `build/dmg-staging/Watchtower.app` — named a live-copy source by the diff's own plist comment — and standalone `build/watchtower`) plus the fail-open `|| true` on the ps pipeline; test-analyzer alone caught the isTerminated gap. [9/7]
- weak-dimension: 7 again — six green tests pin a near-tautological one-line pure helper while every load-bearing artifact of the fix (plist key, activate-then-exit wiring, script guard) is unasserted; verbatim recurrence of the 2026-06-19 "unasserted load-bearing static flag" shape, and the tests structurally cannot fail on the F2 race the helper's signature can't even express.
- rule-gap (candidates): (a) a mutual-deference singleton guard (each newcomer defers to "the other") must impose a deterministic total order (strictly-older launchDate, pid tie-break, skip terminating peers) — symmetric deference yields zero survivors under simultaneous launch, dim 6/9; (b) a pre-flight guard protecting a destructive command must match at least the destructive command's full blast radius (guard pattern ⊇ rm -rf target), dim 9; (c) `exit(0)`-before-run-loop paths must emit one log line first — a silent process exit is the process-level swallowed error, dim 9.
- outcome: TBD

## 2026-08-05 — PR #61 feature/jira-multi-account (round 2, after 1c8a2909 closed round 1) — verdict: changes-needed

- contested (judge escalation over a converged consensus): the debate agreed on a five-item must-fix list but rated everything "major" and filed **zero blockers**. Two of the five are promoted to blocker, each on an existing precedent rather than intuition. (a) **F3** — `connectJiraAccount` (`cmd/jira.go:352-360` → `:417`) falls through `selectJiraSite`'s `preferCloudID` loop when the account's old cloud id is no longer in the grant's resources, then auto-selects on `len(resources)==1` and calls `UpdateJiraAccountConnection` unconditionally, so the Desktop **Re-login** button silently re-points an account at a different Atlassian site while that site's old rows keep the same `account_id`; under this PR's new composite `(account_id, key)` PKs the two sites' issues then overwrite each other. Decisive: this branch **states the opposite rule in its own code** — `resolveUnambiguousJiraIssue` (`internal/db/targets_jira.go:75-83`) documents that a WRITE path must never guess on cross-site ambiguity because "guessing there would silently mutate the owner's state from the wrong site's issue". A branch violating a principle it authored three files away is not a severity judgement call. (b) **CR1** — `JiraBoardProfileView.swift` (untouched by the diff) spawns four CLI actions without `--account` (`:583` analyze, `:639/:665/:691` override) while its already-fixed sibling `JiraBoardsSettingsView.swift:221/:301` passes it; all four call `resolveJiraAccount`, which hard-errors with ≥2 enabled sites, and three of them use `try? process.run()` with an unread stderr `Pipe()` and no exit-code check. Exactly the PR #58 `--app-return` blocker shape (a Desktop affordance silently dropping a required CLI argument for 100% of multi-account users), on the feature's headline screen. [6 AC / 8 regression]
- contested: prosecutor round 2 escalated CR1's READ half (`JiraQueries.fetchBoard`, `:146-154`, `.order(Column("account_id")).fetchOne` → lowest account wins) into the same must-fix. **Split: write half blocker, read half minor.** Spec non-goal #2 explicitly documents `fetchBoard`'s bare-id resolution as an accepted v1 deviation; a documented deviation and an undocumented one cannot carry the same severity even when they sit in the same file. The read half stays chargeable only because the caller (`:561-562`) holds `board.accountID` — the documentation legitimises an avoidable ambiguity, which is a weaker charge than a silent wrong write.
- pattern (judge-added; no single lane had it whole, and the run's most transferable finding): **FOUR false-prose sites in one branch**, three shipped in the same commits as the code they misdescribe. (1) spec:104-107 claims `wireJiraSyncers` clears state to "ok" — its only write is "error" (F9/CR9). (2) The same spec's **non-goal #2** claims "the boards screen passes `--account` explicitly on sync / select / **analyze**" — contradicted by `JiraBoardProfileView.swift:583`; **no lane connected this sentence to CR1**, so the PR ships a paragraph that asserts its own blocker away. (3) `daemon.go:465-466` promises "an already-failing account is not re-stamped" — that guard exists only in `wireJiraSyncers` (`cmd/sync.go:509`), never in `phaseJiraSync` (S1/SF5). (4) `JiraAccountsViewModel.swift:11-12` justifies the missing `--app-return` with "Jira's CLI opens the browser itself" — a non-sequitur: the flag governs where the OAuth **success page** redirects (`watchtower-auth://`), not who opens the browser, and the slack/google CLIs open it themselves too (verified on `origin/main`: calendar, gmail, google, outlook and slack all register it; Jira alone does not). This is the THIRD consecutive round on this branch of the lesson written in its own 2026-08-03 entry. The correction: grep the claim's **subject** (function name, flag name) across spec + Go comments + Swift doc comments, not the sentence. [3 code style / 6 AC]
- miss (a prior round's finding, not a new one): F4 (`--account` accepted-and-ignored by the four dashboard commands) is the 2026-08-03 entry's own re-scoped F15, left unfixed by 1c8a2909. What is genuinely new is that the releases dashboard is **actively wrong**, not merely untidy: `jira_releases` is properly account-scoped (both PK and UNIQUE carry `account_id`, `schema.sql:1224-1236`) while `GetJiraIssuesByFixVersion(rel.Name)` (`internal/db/jira_dashboards.go:465`) matches on version NAME across all accounts, so two sites shipping "v1.0" list twice and share one merged progress percentage. Lesson: a prior round's *flag-hygiene* finding must be re-derived against the schema the same PR introduced — the hygiene framing hid a wrong-number bug for a full round.
- weak-dimension: **7 (test quality, 6.5/10)** — the familiar bimodal shape with an unusually pointed tell: migration 00049 carries six dedicated fixture tests, while `ErrAuthRevoked` — the sentinel the PREVIOUS round added specifically to stop this feature being inert — can be reverted at `internal/jira/sync.go:118-124` with the suite still green, and the CLI resolution helpers every subcommand routes through (`resolveJiraAccount`, `selectJiraSite`, `connectJiraAccount`'s rollback) have zero tests. **Both blockers live inside exactly that untested band** — the coverage gap and the defect distribution are the same map, which is the actionable form of the observation.
- false-positive: none dismissed outright — but four adjustments, every one a debater correcting *itself* on fresh evidence rather than adjudication: F5 → nit (the printed table has an `Acct` column), F6 split to Jira-specific residue (Slack-inherited half excluded), F8 re-widened by the prosecutor's own correction (multi-project boards store `project_key=''` and `continue` before `syncWithJQL`, so their 401s reach only the log-and-swallow `SyncSprints`/`syncReleases` → that account's green is *permanent*, never self-healing), and F12 kept minor while its fix moved from doc-only to alias scoping on `reflectChurnThreshold = 3` evidence (`internal/memory/reflect.go:43`). The prosecutor also withdrew its own round-1 clean bill on migration step 2 (SF7: `NO TRANSACTION` plus a seed `INSERT` guarded only by "does legacy data exist", never "does the row already exist"). A round whose adjustments are self-corrections is working as designed; record it as the healthy shape.
- process: **codex lane UNAVAILABLE** (CLI 0.23.0 cannot request the account's gpt-5.5 model) — second consecutive round without it on this branch. Last round all four `pr-review-toolkit` specialists were also down; this round all four ran, and `code-reviewer` **alone** carried the second blocker (CR1), which no debater found in round 1 and which the prosecutor could only correct itself into during round 2. That is the counterweight to the standing "codex carries the unique correctness catch" note: the specialist lanes are not redundant with the debate. With codex down, the Go read-model surface is this run's least-covered area — state the residual risk rather than letting five green CI checks imply otherwise.
- process (housekeeping, for whoever merges): this branch's `docs/review/review-rules.md` (73 lines) predates main's promoted **"Swift / Desktop conventions"** and **"Review process"** sections (124 lines), and its `review-lessons.md` is missing main's six 2026-08-04 entries. I reviewed against main's fuller, current ruleset — several findings above cite promoted rules the branch's own copy does not yet contain (notably the CLI-flag-registration rule that CR1 mirrors). Expect a tail conflict in both files on merge; take main's side and re-append this block.
- rule-gap (candidates): (a) an account-scoping change to a Desktop domain must be closed by a mechanical `grep -L -- '--account'` over **every** Swift file that spawns that domain's CLI — the settings-list/detail-view file pair is the recurring shape, and touching only one of the pair passes build, lint and CI, dim 8; (b) "documented v1 ambiguity" is a legitimate defence only where the caller genuinely lacks the disambiguator — when the calling view already holds `accountID`, the documentation legitimises an avoidable defect and should be read as a finding, not a waiver, dim 2; (c) a `NO TRANSACTION` goose migration whose seed `INSERT` is guarded only by "does legacy data exist" must additionally guard on "does the row already exist", or a crash before the version is recorded mints a phantom account per retry (00048 is transactional, 00049 is not), dim 8.
- outcome: TBD

## 2026-08-04 — feature/calendar-now-line — verdict: changes-needed

- contested: codex's sole finding (isToday captured outside TimelineView → cross-midnight staleness) filed as major — settled NIT: judge-verified `CalendarViewModel.swift:162-167` runs a 30 s `loadEvents` loop, so `dailyEvents` mutates, `daySection` re-evaluates `isToday`, and the marker self-heals within ≤30 s of midnight. The prosecutor had explicitly checked and declined to file the same item; codex didn't account for the poll loop. A lane filing "stale UI" against a screen with a periodic reload must first check the VM's observation/poll cadence. [6 AC]
- contested: jump-capsule hit-zone (padding/background applied AFTER `Button` with `.buttonStyle(.plain)` → most of the painted red capsule is click-dead) — raised ONLY by the two specialist lanes (code-simplifier + toolkit code-reviewer conf 90), missed by both debaters. Settled MAJOR: the capsule is the feature's headline affordance and the in-file reference pattern (`allDayChip`) already does it correctly; a one-line move of styling inside `label:`. [6 AC / 5 fit]
- false-positive: none dismissed outright this run — small diff, high lane agreement; the only demotion was the codex midnight major (above).
- miss: dangling now-line under an all-day-only Today (`timed=[]` → `lineIndex == 0 == count` → bare marker row renders) caught ONLY by pr-test-analyzer — an unspecced branch between the spec's covered cases; neither debater nor codex walked the degenerate-input path of the render loop despite the tests covering it for the pure helper. Recurring shape of the "test degenerate clean-exit branches" memory lesson, this time in a @ViewBuilder. [6 AC / 7 tests]
- weak-dimension: 7 (test quality), but in the bimodal shape again — the pure helper got 5 genuine relative-date tests (good), while the render half (marker-insertion loop with a duplicated boundary condition, visibility trichotomy) shipped with zero tests and the logic left inline in @ViewBuilder even though the diff's own NowLine comment states the extract-for-testability rationale. The tell: a diff that creates a pure-helper file for HALF its logic and inlines the other half.
- rule-gap (candidates): (a) SwiftUI — modifiers applied outside a `Button` with `.buttonStyle(.plain)` are non-interactive chrome; padding/background must live inside `label:` (allDayChip is the house pattern), dim 5/6; (b) an `onPreferenceChange` fed by per-scroll-tick geometry must store a derived discrete classification (enum/bool) and write only on change, never the raw CGRect into @State on a non-lazy tree, dim 4; (c) `PreferenceKey.defaultValue` should be `static let` — `static var` is mutable shared state under Swift 6 strict concurrency, dim 3.
- outcome: TBD

## 2026-08-03 — PR #61 feature/jira-multi-account (sub-project 3 of 3, migration 00049) — verdict: changes-needed

- contested: F1 (revoked grant reaches no durable surface) — orchestrator asked whether doc-only closure is acceptable. Settled YES, follow-up not blocker: judge-verified via `git show 3e56935:internal/db/jira.go:446` that `UpdateJiraSyncState` has NEVER written `last_error`/`last_error_at`, so the swallow is inherited verbatim, and `wireJiraSyncers` still catches a *missing* token while Re-login stays unconditionally reachable. BUT the fix batch corrected only the spec and left the **newly-written** `phaseJiraSync` doc comment (`internal/daemon/daemon.go:456`) still asserting "it lands in that project's jira_sync_state row" — verified NEW in this PR (main's comment was two lines). A fix batch that corrects prose in the spec must grep the same false sentence in the code comments it shipped in the same commit; the code comment is the more-read of the two and now contradicts the paragraph added three lines below it. [9 error-handling / 3 code-style]
- contested: F15 scope — prosecutor filed "`--account` honored by only ~6 of ~20 subcommands". Re-scoped after enumeration: every genuinely site-scoped subcommand DOES resolve it (boards, select/deselect, sync, boards analyze, override, fields, discover, fields map, login = 9 verified sites); the real residual is the four dashboard commands in the untouched `cmd/jira_dashboards.go`, which inherit `--account` as a cobra **persistent** flag and silently ignore it while aggregating across all sites. Lesson: a persistent flag registered on a parent command is silently accepted by every child — enumerating "who reads it" is not enough, you must also enumerate "who accepts but ignores it," and an accepted-then-ignored flag is worse than a rejected one. [2 architecture-decision]
- contested: F11 (composite-PK row duplication) — ledger deferred it uninvestigated; judge resolved as real-but-bounded MINOR. `GetJiraIssuesForTrack`/`ForDigest` still `SELECT DISTINCT`, but the column list now leads with `account_id`, so two sites' same-key issues no longer collapse (a track can list OPS-42 twice); and `GetJiraIssuesByKeys`' `issueMap[iss.Key]` silently keeps the last colliding row. Both are the documented v1 key-ambiguity in a new shape, not new corruption — but the spec documents the *principle* and never enumerates the *surfaces*. [8 regression]
- false-positive: F16's implied perf half — `readSiteURL()`'s new synchronous main-thread `pool.read` per `.onAppear` looks like a fresh main-thread-DB finding, but `git show 3e56935:.../JiraKeyExtractor.swift` shows the pre-change body did a per-card file read + full YAML parse on the same hook. Strictly cheaper than what shipped. Only the `nonisolated(unsafe)` static (verified the target's first and only, by grep) survives, as a concurrency-hygiene minor with a one-word fix (`@MainActor` on the enum). Severity must be measured against what the call site did BEFORE, not against the ideal. [4 efficiency / 1 architecture]
- miss (judge-only, and the verdict driver): the two most consequential fixes in the review-fix commit — `resolveUnambiguousJiraIssue` (F3, a WRITE path that flips user-authored target status) and the `phaseJiraSync` ctx-cancel guard (F5) — shipped with **zero** regression tests. `TestSyncJiraTargetStatuses`/`_MissingIssue` are single-account and pass identically pre- and post-fix; reverting F3 to `GetJiraIssueByKey` leaves the suite green. Neither debater, nor the style lane, nor the orchestrator's own gate run (build/vet/lint/test all green) noticed. Rule §6 is explicit and was simply not applied to the fix batch. This is the 2026-07-04 lesson recurring in a new place: **fix batches need the same §6 discipline as features**, and "all suites green" is exactly the signal that hides an untested fix. [7 tests / 6 AC]
- weak-dimension: 7 (test quality) again, but with a NEW shape worth naming — not the usual bimodal "library tested, entry point bare." Here the *net-new* surface is genuinely well covered (6 migration tests incl. Up/Down cycle and non-issue-data seeding, 7 legacy-seed cases, 4 daemon fan-out, 7 account CRUD), and the hole is precisely the review-driven deltas. Coverage tracked the plan, not the review.
- process: three lanes down at once — codex UNAVAILABLE (CLI absent, exit 127), all four pr-review-toolkit specialists UNAVAILABLE (plugin agents absent), and no round-2 rebuttal (advocate stalled ~35 min and returned after dispatch). The lessons log records codex carrying unique correctness catches in 5+ prior entries and pr-test-analyzer carrying the strongest coverage finding in ~4 consecutive ones — and the coverage gap above is exactly what pr-test-analyzer would have filed. Judge compensated by reading every changed file in the Go jira/db/daemon/memory/cmd surface and re-running the suites uncached, but the entire Swift half (4 changed sources, 2 new test files) is unverified by any compiler in this environment. Residual-risk confidence materially lower than the finding count suggests; say so in the report rather than letting green gates imply otherwise.
- rule-gap (candidates): (a) promote — a review-fix commit is a change like any other, so rule §6 applies to it: each behavior fix in the batch needs a test that fails on the pre-fix code, and the batch's own "all green" run is not evidence (this run: 2 of 8 behavior fixes untested); (b) a cobra **persistent** flag is auto-accepted by every descendant — adding one requires auditing which children ignore it and rejecting it there, dim 2; (c) when a fix batch corrects a factually-wrong claim in a spec, grep the same claim across doc comments shipped in the SAME commit — half-corrected prose reads worse than the original because it now self-contradicts, dim 3/9.
- outcome: TBD

## 2026-07-26 — working-tree (multi-account IMAP + Outlook email source, first-of-kind architecture) — verdict: changes-needed

- contested: F1 severity (prosecutor blocker vs advocate major) — settled MAJOR: verified in code that `internal/gmail/sync.go`'s `maxSeen` watermark has the EXACT same shape (advances past a failed upsert if a later message in the batch succeeds at a higher timestamp/UID) already shipped and accepted on main. A newly-added second syncer inheriting an already-accepted pre-existing defect is major, not blocker — but the fix should land for both syncers together, not just the new one, since filing it against only the new code would let the original instance survive untouched. [6 AC / 9 error-handling]
- contested: F7 severity (minor→major upgrade, mutual agreement) — settled MAJOR, judge-verified: `cmd/imap.go:122-134` calls `CreateEmailAccount` then `store.Save(credentials)` with no rollback; confirmed this is NOT inherited from gmail (gmail has no two-step row+file sequence). Compounds with F6 (initial `store.Load()` failure paths never call `SetEmailAccountAuthState`, unlike the sibling `RefreshFunc` a few lines below in the same file) into a permanently-invisible "ok"-status ghost account with no credentials and no error surfaced anywhere — verified both code paths directly (`cmd/sync.go:527-531`, `557-559` vs `572-574`). [8 regression / 9 error-handling]
- contested: F2 — no debate needed, but worth recording as the strongest 3-lane (prosecutor, codex, general-reviewer stand-in) plus judge-verified convergence this cycle: `imap_messages` PK `(account_id, uid)` and the inbox dedup key `(channel_id, message_ts, trigger_type)` both omit `uidvalidity` despite the migration's own comment stating a UIDVALIDITY change means UIDs "no longer mean anything" — verified `internal/db/migrations/00022_email_accounts.sql:40` and `internal/inbox/imap_detector.go:99-105` directly. A textbook RFC-3501-permitted UID-reuse scenario silently overwrites an old row and permanently suppresses the new message from ever reaching the inbox. BLOCKER, no dissent. [6 AC / regression]
- false-positive-adjacent (settled, not dropped): plaintext IMAP/Outlook credential storage (`internal/imap/credentials.go:18-21`) — the doc comment explicitly claims parity with the already-accepted `gmail_token.json` risk model. Judged NOT a new blocker/needs-human item: this is an existing, named, accepted precedent in the same codebase, not a novel security regression. Recorded here rather than silently dropped so a future reviewer doesn't have to re-derive the same reasoning.
- miss (specialist-lane-unique, folded into final verdict): (a) pr-test-analyzer alone caught `db.DeleteEmailAccount` has zero coverage anywhere (reachable only through the untested CLI remove/logout commands) and `extractPlainText`'s MIME walk is exercised only by a single-part plain-text case (no multipart/HTML/malformed-MIME test) — judge-verified via grep, both real gaps, neither in the debate ledger. (b) code-simplifier alone caught `internal/imap/outlook_auth.go`'s `PrepareOutlook`/`Complete`/`MicrosoftPrepareResult` (~80 new lines) have zero production callers — judge-verified only `outlook_auth_test.go` calls them; `cmd/outlook.go` uses the self-contained `LoginOutlook` instead. Noted `gmail.Prepare`/`gmail.Complete` are ALSO dead (pre-existing), so this is an established anti-pattern being repeated rather than introduced fresh — still a rule §2 violation, rated minor not major given the precedent. (c) general-reviewer stand-in alone caught the missing Outlook-equivalent of gmail's `validateGrantedScopes`: `cmd/outlook.go:90-98` only guards empty `refresh_token`, not a token that has one but was granted without the IMAP scope specifically — judge-verified; rated minor since the eventual failure (opaque XOAUTH2 auth error) still surfaces as a visible auth-state error, just with a worse message, unlike F13's forever-silent permalink gap.
- weak-dimension: 7 (test quality) again — `cmd/imap.go`/`cmd/outlook.go` (the actual user/Desktop-invoked entry points) and the entire Swift `EmailAccountsViewModel`/`EmailAccountQueries`/`EmailAccount` surface have zero tests, breaking this codebase's own established per-ViewModel test convention; meanwhile `internal/imap` unit tests are reasonably thorough at the client/auth layer. Same bimodal-coverage pattern as prior entries: strong coverage at the library layer, zero at the two thinnest-looking-but-highest-blast-radius layers (CLI entrypoints, Desktop ViewModel).
- rule-gap (candidates): (a) a capped fetch-then-cap pipeline (`FetchSince`/`FetchNewSince` then client-side cap) should cap BEFORE the expensive per-message body fetch, not after — mirror `gmail.Syncer`'s list-cheaply/fetch-only-the-capped-set shape explicitly already documented in-repo as the reference pattern, dim 4; (b) a resync-on-mailbox-recreation design (UIDVALIDITY reset) must extend every dedup/PK key that assumed UID stability to include the validity epoch, or the reset itself becomes the vulnerability — this is a new, sharper restatement of "watermark/cursor reset must be total over every downstream key that assumed monotonicity," dim 6/8; (c) a two-step "create DB row, then persist credential file" account-provisioning flow needs either a single transaction-like rollback (delete the row if Save fails) or the row must start in an explicit `pending`/`unconfigured` status that daemon wiring visibly flags rather than defaulting to `ok`, dim 8/9.
- outcome: TBD

## 2026-07-26 — working-tree (multi-account IMAP + Outlook, convergence loop, rounds 2–3 + final judge synthesis) — verdict: approve

- contested: whether round-1's own severity calls would survive independent re-scrutiny two rounds later. They did: F1 (BLOCKER, uidvalidity PK/dedup) and F7 (MAJOR, create-row-then-save-credentials rollback) were re-verified in code this pass exactly as fixed (`internal/db/migrations/00022_email_accounts.sql` PK is `(account_id, uidvalidity, uid)`, `internal/inbox/imap_detector.go` channel_id embeds the uidvalidity epoch, `internal/imap/sync.go`'s `stalled` flag genuinely stops `maxUID` advancement at the first upsert failure, `cmd/imap.go`'s `createEmailAccountWithCredentials` rolls back via `DeleteEmailAccount`). Judge independently re-ran `go build/vet/gofmt`, the targeted `-race` suites, and re-read every changed hunk rather than trusting the round-3 summary; nothing regressed and nothing was claimed-but-absent. [6 AC / 8 regression]
- contested: codex's round-2 severity escalation of two round-1-deferred items (phaseImapSync per-account timeout, refresh-token persist-failure retry) to "major." Resettled at round-1's original rating (self-limiting / bounded by go-imap's own read timeouts, no new evidence offered beyond re-asserting severity) — a severity re-litigation without new evidence doesn't reopen a deliberately-deferred, reasoned item. [8 regression / 9 error-handling]
- false-positive-adjacent (round 1, reconfirmed): none newly dismissed this pass — round 2/3 surfaced only two small NEW items (client.go snippet mid-rune UTF-8 slice; untestable inline outlook login validate-then-persist), both fixed and verified, no false positives among them.
- miss: none this pass — codex (round 2) and the prosecutor (round 3) each independently converged on genuinely real, small residual issues (the UTF-8 slice bug, the missing pre/post-fix regression test for it) rather than surfacing anything the other lane missed; the fix batches stayed proportionate to the findings.
- weak-dimension: none new — dimension 7 (test quality) was this branch's strength, not its weak spot, across all three rounds (uidvalidity regression test, watermark-stall test, both outlook helper-extraction tests, and the round-3 truncateUTF8/end-to-end pair all judge-verified present and load-bearing).
- rule-gap (candidate, promote): the round-3 catch is itself a clean instance of rule §6's discipline taken further than usual — the orchestrator didn't just assert "the new test would have failed pre-fix," it MECHANICALLY reverted the fix, ran the new end-to-end test, captured a real "not valid UTF-8" failure with the corrupted string printed, then reapplied the fix and reran clean. Worth promoting as an explicit companion to §6: for a subtle data-corruption fix (not just a wrong-return-value fix), prefer an actual revert-run-reapply cycle over reasoning-by-inspection that a test "would" fail. dim 6/7. [promoted 2026-08-04 → review-rules §6]
- process note: the Permalink finding (round 1) is a good worked example of avoiding over-engineering — instead of inventing a fake deep-link scheme to satisfy the letter of the finding, the orchestrator checked the actual Swift call site (`DashboardViewModel.swift`'s `guard !item.permalink.isEmpty else { return nil }`) and found the empty-string case already degrades gracefully, then rescoped the fix to a doc-comment clarification. A severity correction driven by reading the actual runtime consumer, not the abstract description of the bug, belongs in the toolkit as a repeatable move.
- convergence (FINAL): 3-round loop closed clean. Round 1: 1 blocker + 7 major + 6 minor + 2 nit, all fixed and orchestrator-verified. Round 2 (codex + prosecutor re-verification of the fix batch): all 10 round-1 fixes confirmed genuinely closed in code; 2 new minors surfaced (UTF-8 snippet slice, untestable outlook login), both fixed. Round 3 (narrow re-check of round 2): 1 finding (missing regression test for the UTF-8 fix per rule §6), fixed with a revert-verified test. Judge's own independent pass this round re-read the uidvalidity migration/detector, the watermark-stall loop, and both new test files, and independently re-ran `go build`, `go vet`, `gofmt -l`, `golangci-lint` (clean except the pre-existing, untouched-file G705 gosec findings), and the targeted `-race` suites (`internal/imap`, `internal/inbox` incl. `TestDetectImapAccountsUIDValidityResetDoesNotCollide`, `cmd` incl. `TestFinishOutlookLogin_*`) — all green, matching the round-3 summary with zero discrepancies found. Accepted findings remaining after this pass: zero. Ready to merge; only pre-existing, explicitly-deferred-with-reasoning follow-ups remain (gmail.Syncer's identical watermark pattern, refresh-token retry, phaseImapSync per-account timeout, best-effort credential-delete retry) — none introduced by this diff, none blocking.
- outcome: TBD

## 2026-07-13 — feature/meeting-transcriber — verdict: changes-needed

- contested: F1 severity — debate settled MAJOR (owner-decides semantics); judge ELEVATED to BLOCKER on the 2026-07-04 lost-update precedent. cmd/meeting_transcript.go:210-211 unconditionally `UpsertMeetingRecap(eventID, transcriptText, recapJSON)` on the event-linked path, silently overwriting a pre-existing user `recap_json` + `source_text`; the correct guard pattern already exists in-repo on the Swift twin (MeetingTranscriptQueries.swift:41, `guard fetch(eventID)==nil else { return }`). A Go-side write that clobbers user-approved data while its Swift mirror guards the same collision is the exact shape elevated to blocker on PR#22. The "recording is authoritative" reading is defensible for `recap_json` but does NOT license silently discarding `source_text` with no `--force` and no doc. The *semantics choice* (mirror-the-guard vs documented --force overwrite) is the only part that goes to the owner; that SOME guard must exist, shipping with the F5 collision test, is not optional. [8 regression / 6 AC / 9 error-handling]
- contested: F2 severity (major-prosecutor / minor-advocate) — settled MINOR. The split-decision function `generateArgs` (where threshold/off-by-one bugs would live) is covered by three tests incl. the exact StdinThreshold boundary and the >32KB→stdin case; the only uncovered residual is the one-line exec plumbing `cmd.Stdin = strings.NewReader(stdin)`, which fails LOUDLY on break (empty stdin → EOF → empty/garbage recap), not as a silent happy-path wrong-answer. pr-test-analyzer (8.5/10) did NOT rank stdin wiring among its gaps — the dedicated coverage lane's silence is signal it isn't major. Should still ship a fake-binary test through the claudePath/codexPath seam (rule §7 permits fakes). [7 tests]
- miss (codex-unique): CX-1 — TranscriptSectionView.swift:43-45 `.onChange(phase==.idle)` calls only `load()`, never `onChanged()`, so an open event's recap section stays stale/missing after a recording completes until the event is reopened (the file's own doc says onChanged refreshes the host recap). The flagship happy path (record → recap appears) is broken and NO other lane caught it — recurring pattern: codex carries unique correctness catches on this repo. [6 AC]
- miss (lane-unique): SF-3 — silent-failure-hunter alone caught that RecordingIndicatorView.swift:88 `stop()` guards on `ProcessCLIRunner.makeDefault()` and no-ops if nil, making live audio/system capture unstoppable from the UI (privacy-relevant) — the runner is only needed at the later save step. Decoupling stop from the runner is the fix. [9 error-handling / 6 AC]
- false-positive: F6 (pipeline_runs "auto" model — matches 11/16 call sites, dominant convention); F8 (prompt-version bump clobber — UpdatePrompt increments version so customized rows are ≥2 and the Seed `<2` guard never fires); F9 (audio-input entitlement inert / unsandboxed dep — TCC is the real gate, pre-existing + load-bearing). All three correctly dropped. [2 / 8 / 9]
- weak-dimension: 7 (test quality) — again, but note the split: the Center's failure paths (`FakeRecorder.stopError` exists but is UNUSED; recorder.stop/engineFactory/decode throws must land `.failed` with pendingAudioURL kept) are the retry-safety core and are untested, while the surrounding suite is green. Green-masks-the-untested-core recurs; the tell this time is a seeded-but-unexercised fake error field.
- rule-gap (candidates): (a) a Go write to a table also written by a Swift Queries twin must mirror the twin's collision guard (or document divergent semantics + ship the collision test) — the Swift guard's existence is the auto-flag, dim 8; (b) an audio/media capture Stop control must not depend on a resource only the downstream save step needs — gate the save, not the stop, dim 6/9; (c) a `.onChange`/refresh hook that must propagate to a host view has to call the documented host-refresh (onChanged), not only the local reload — assert the host state updates, dim 6. [(a),(c) promoted 2026-08-04 → review-rules "Swift / Desktop conventions"]
- convergence (FINAL verdict: approve): all accepted findings fixed in two batches; a re-run of all seven lanes surfaced 5 more small ones (R-1 alloc latch, R-2 prepareRetry sidecar discard, CX-3 event-link crash recovery, 2 pinning tests) all fixed; a final verification round (prosecutor + silent-failure + codex over the two point commits) returned unanimous no-findings/clean, advocate confirmed faithful, general reviewer merge-able, full gates green (go build/vet/test, swift build/test, both linters, -race on touched pkgs). Judge verified the blocker CLOSED in code not just claimed: F1 now looks up `GetMeetingRecap(eventID)` and only writes `meeting_recaps` when `existing == nil` (guard-and-skip per owner decision, lookup-error → safe summary_json fallback), cmd/meeting_transcript.go:227-239; L-3 fixed alongside (`SetPromptStore` wired :205); CX-1 idle branch now calls `onChanged()` (TranscriptSectionView.swift:47-49); SF-3 stop decoupled from the runner with an explanatory comment (RecordingIndicatorView.swift:87-92). Needs-human both resolved by owner: F1=guard-like-Swift, SF-6=CAF container. Deferred as follow-ups (accepted): list_transcripts N+1, S-4/S-5 cosmetics, view-layer ViewInspector tests, degraded-envelope test, M-1 sidecar surface, M-2 cross-language recordings-dir mirror (mitigated by contract test). Manual acceptance (TCC, live meeting, model download, AudioFileDecoder) always out of review scope, documented + pending.

## 2026-07-15 — feature/secretary-memory (phases 0–2, memory vault + extraction) — verdict: changes-needed

- contested: F5 severity (prosecutor major vs advocate minor) — settled MAJOR: production wires logf=nil for BOTH daemon and CLI (cmd/memory.go:67, cmd/sync.go:298) while every sibling pipeline in the same daemon block receives `logger`; that single nil silences window-failure reasons, watermark-write failures, and run-accounting failures, and pipeline_steps has no error column, so the reason exists NOWHERE. Not a degraded log line but a new pipeline's entire diagnostic layer wired to /dev/null against the sibling convention; diagnosis of the poison-window and bad-file majors depends on it. Not blocker only because the failure FACTS are durably recorded (steps status='error', WindowsFailed, error_msg). [9 error-handling / 5 fit]
- contested: A-1 elevated agreed-major → BLOCKER — shape-degenerate extractor JSON (missing/misnamed `refs` key) yields zero-ref episodes that validateRefs discards with dropped==0; the window reads as routine chatter and the watermark advances, silently losing content. Same class as F1 (silent permanent skip) and a verbatim recurrence of the 2026-06-25 lesson: an unmarshal-only parser must key success off affirmative shape, never off absence of a field. Fix must land inside the F1 watermark rework. [6 AC]
- contested: HIGH-2 (single-lane, silent-failure stand-in; debaters never adjudicated) — judge-verified REAL and MAJOR: validateRefs (extract.go:104-110) conflates a DB lookup error with not-found, so a transient SQLite error (realistic given F2's missing cross-process lock) drops all refs, discards the episodes, returns nil, marks the window done, and ADVANCES the watermark — MEM-01's fail-closed becomes MEM-04's fail-open exactly when the DB is unhealthy. The in-code comment documents the wrong composed effect. [9 / 6]
- false-positive: F14 dismissed as a security finding — no new trust boundary (vault content feeds no prompt in this diff; MCP surfaces are read-only); retained only as a Phase 4 spec doc note on provenance framing. Also MED-3 softened: ingest's discarded rejected-count self-repairs because F10's rescan-forever loop re-renders provenance next run — an inefficiency finding acting as the safety net for an error-handling one. [9]
- miss: codex UNAVAILABLE (CLI not installed on this machine) — the lane that historically carries unique silent-wrong-answer catches. Its role was partly covered by the stand-ins: G6 (INNER JOIN users silently and permanently skips messages of never-synced/deleted authors while the watermark passes them — judge-verified, ruled MAJOR) came only from the test-analyzer stand-in; three of the four rulings I had to verify in code (HIGH-2, M3, G6) were single-lane. [6 AC]
- weak-dimension: 7 (test quality) — the F1 guard tests seed fixtures 60 seconds apart, structurally unable to hit the same-second tie hole. This is the THIRD occurrence of the 2026-07-04 tie-safety lesson on this repo: a capped window + timestamp watermark was again shipped without ORDER BY ts,id tie-draining or a tie-seeding test. Promote candidate (a) from 2026-07-04 to review-rules.
- rule-gap (candidates): (a) tie-safety rule — third recurrence, promote; (b) a new pipeline wired into the daemon block must receive the same logger its siblings get — a nil logf in a production factory is a wiring defect, not a style choice (F5 class), dim 5/9; (c) an existence-check helper used for validation must distinguish `(false, nil)` from `(false, err)` at every call site — fail the operation on err, not the datum (HIGH-2 class), dim 9. [(a) promoted 2026-08-04 → review-rules §6; (b),(c) promoted 2026-08-04 → review-rules §9]
- process: pr-review-toolkit plugin agents unavailable this session; four general-purpose stand-ins ran the same lenses and performed strongly (test-analyzer: G6/G7; silent-failure: HIGH-2 — both judge-confirmed). Stand-in panel is a viable fallback but needed judge code-verification on every single-lane major.
- outcome: TBD

## 2026-07-04 — PR #22 feature/task-ai-agent (convergence round 2, fix batch 5d347e2) — verdict: changes-needed

- contested: R2-F1 severity — codex filed minor, prosecutor blocker. Settled BLOCKER: the round-1 watermark fix (advance to CappedAt, next window `> ?`) closed the multi-second case but designed in a same-second tie hole with no id tiebreaker; the realistic cold-start shape (inbox 7-day lookback batch-inserts up to 100 rows sharing one strftime second vs the 40/source cap) permanently skips rows 41+. A fix that re-opens the same silent-permanent-skip class it was charged with is not closed. The guard test seeded strictly increasing seconds — the fix's OWN regression test structurally cannot catch its residual. Lesson: when a fix is watermark/cursor-shaped, the re-review must attack the tie/boundary case at the cursor's resolution, and the guard test must seed ties. [6 AC / 7 tests]
- contested: R2-F4 semantics — Swift UTC unification was the correct round-1 fix, but it exposed the shared residual: date-only due dates go overdue at UTC START of day (Go prefix string-compare `due_date <= cutoff`), and isDueToday feeds a UTC instant into Calendar.current. Judge ruled: date-only = overdue at END-of-day UTC, both sides via day-string comparison; local-EOD left as an owner product call. Charged PR-scope major (both predicates branch-introduced), not a re-opened drift blocker. [6 AC]
- contested: R2-F13 scope — Swift updateStatus lacks the INBOX-02 cascade Go introduced in this PR (16e353d). Ruled required pre-merge: project memory's "catch-up ack dual path" convention makes Desktop mirrors mandatory for cascades, Desktop Done is the dominant path, and no daemon phase repairs target_due. A Go-side cascade added without its Swift twin should be auto-flagged in round 1, not surface in round 2. [8 regression / 5 fit]
- miss (round 1): both blockers of this round are FIX-INTRODUCED — the boundary tie was designed into the F2 remedy, and migration 00007's UNIQUE collision (malformed twins sharing source/target/relation with different URLs, no clean '' twin → UPDATE blanks both → db.Open bricked; 4 lanes, reproduced twice) was written as the F6 remedy. Fix batches need the same adversarial depth as features; round-1 verdict prose should demand tie-safety and migration-totality explicitly when prescribing cursor fixes and data-cleanup migrations.
- false-positive: simplifier's dead-nil-guard (cmd/targets_ai.go events==nil) dismissed keep-it — belt-and-braces for the promoted nil-slice→null rule; deleting it re-exposes the recurring class. [9]
- lane balance: codex healthy this round (fast-mode, small diff) and independently converged on both blockers — diff size, not the lane, was round 1's problem; per-module sharding for huge PRs stands. silent-failure-hunter was the strongest lane (reproduced F3 against real schema, correctly ranked F1 HIGH).
- rule-gap (candidates): (a) a capped window + timestamp watermark must be tie-safe at the timestamp's resolution — ORDER BY ts,id and drain boundary ties, and the guard test must seed >cap same-timestamp rows, dim 6/7; (b) a data-cleanup migration touching a UNIQUE-constrained column must be total over every constraint-permitted shape (prove the UPDATE cannot collide, or delete colliding twins first) + a test seeding the collision, dim 8; (c) any Go-side status/ack cascade must land with its Swift Queries mirror in the same change (dual-path convention), dim 8. [(a) promoted 2026-08-04 → review-rules §6; (c) promoted 2026-08-04 → review-rules "Swift / Desktop conventions"; (b) still open]
- outcome: TBD

## 2026-07-04 — PR #22 feature/task-ai-agent (targets AI agent, 352 files) — verdict: changes-needed

- contested: F5 severity — both debaters settled on major; judge elevated to BLOCKER on rule §9's literal wording: a user-initiated scan whose failure is swallowed at THREE layers (pipeline `nil,nil` on all-observers-fail → CLI prints `null` exit 0 → Swift `(try? decode) ?? []`) is not a documented known-safe case. When each layer's swallow looks individually defensible, severity must be judged on the composed chain, not per-site. [9 error-handling]
- contested: F6 inbox half — advocate had it down to graceful degradation; prosecutor's round-2 persistence evidence (`TargetQueries.swift:201-202` gates only on `hasPrefix("slack:")`, so full-URL refs land durably in `target_links`) flipped it. Settled blocker: durable contract-violating rows need a data cleanup, not just a code fix — persistence is what separates blocker from major in render-garbage findings. [8 regression]
- contested: F2/F4/F3 elevated from agreed-major to blocker — silent permanent data skip on the default path (first-run 7-day lookback vs 40/source cap + unconditional watermark), lost-update clobbering user-approved data when the correct reload-before-write pattern exists in-repo (TargetChatViewModel.swift:318), and cross-language timezone drift. Debater consensus on "major" under-weighted that all three are silent wrong-answers on happy paths. [6 AC]
- false-positive: F18 (ActiveSnapshotLimit reuse) correctly dropped — consistent in-package meaning; F15 correctly withdrawn — triple db.Open + "/sonnet" label verbatim pre-existing on main (git-history check again separated pre-existing from introduced). [2/5]
- miss (process): codex lane UNAVAILABLE — timed out twice at 300s on the 352-file/+49k diff. On this repo's record (2026-06-28: C1, the branch's strongest bug, was codex-only) that lane carries unique silent-wrong-answer catches. Process gap: huge PRs need diff-sharding or per-module codex passes; recorded as Needs-human on observers + nextstep. [6 AC]
- miss (lane balance): the two ≥8 coverage gaps (GenerateAllNextSteps zero tests; watermark-hold contract unasserted) came from pr-test-analyzer, not the debate — and both sit exactly on the code the F2 fix must touch. Third consecutive review where the strongest coverage findings came from the specialist lane. [7 tests]
- weak-dimension: 7 (test quality), seventh consecutive entry — nextstep 6/10, a daemon-every-cycle path with zero tests, while catchup/mcp sat at 9/10 in the same PR. Coverage is bimodal per-module; green-overall keeps masking the untested core.
- rule-gap (candidates to promote): (a) any string datetime column read by both Go and Swift must pin an explicit timezone in BOTH formatters — a sibling formatter that sets UTC while the used one doesn't (Target.swift datetimeFormatter vs periodFormatter) is the tell, dim 6/8; (b) a ViewModel read-modify-write of a whole JSON column must reload the row immediately before writing (the TargetChatViewModel pattern), dim 9; (c) a capped query (LIMIT N newest-first) paired with an unconditional watermark advance is a lossy pair — advancing `last_run_at` requires proof the window was fully consumed, dim 6; (d) the 2026-06-16 nil-slice→`null` rule recurred verbatim (F11, runTargetsObserve) — promote it. [(a),(b),(d) promoted 2026-08-04 → review-rules "Swift / Desktop conventions"; (c) promoted 2026-08-04 → review-rules §6]
- outcome: TBD
## 2026-06-16 — feature/catch-up-summarizer — verdict: changes-needed

- contested: F5 — silent-failure-hunter framed the dropped `parseAIOutput` perr as "removed pipeline_runs telemetry not replaced"; verified against `git show 27c8d8e:cmd/catchup.go` that the old command had NO telemetry, so that premise is false. Settled as a real but narrower minor: an invalid-JSON AI response degrades silently with no log, which is hard to diagnose. By-design degradation is fine; invisible degradation is not. [9 error-handling]
- contested: F7 — prompt example uses singular `"area":"track"` vs plural section areas. Confirmed `refs.area` only feeds the SwiftUI `CatchUpRef.id` string; no matching/clearing depends on it. Demoted to nit/latent, but worth fixing because the prompt teaches the model the wrong vocabulary. [3 code-style]
- false-positive: F8 (stopObserving never called / totalTrackCount unread) — app-lifetime VM, demoted to nit; not load-bearing. [1 architecture]
- miss: F9 (nil Go slice → JSON `null` → Swift non-optional decode throws "Failed to parse catch-up") slipped past the internal panel AND the smoke test (smoke ran with all four areas non-empty so no slice was nil) AND both Go and Swift test suites (Swift fixtures hardcode full arrays; Go tests never assert the wire shape of empty slices). Caught only by codex + debate, empirically reproduced both sides. The single most common runtime path (zero-unread early return) hits it. [6 AC / 7 tests]
- miss: codex first-run badge — `SidebarCountsViewModel` is a NEW file this branch; its only construction site is the `!needsOnboarding` path in `initialize()`, and `completeOnboarding()` never creates it → first-run users see all sidebar badges (incl. Catch-Up) at 0 until restart. Branch-introduced, not pre-existing. Caught only by codex. [6 AC]
- weak-dimension: 7 (test quality) — both Go and Swift suites were green and numerous (807 Swift), yet neither exercised the empty-slice wire shape (F9) nor the SidebarCountsViewModel (untested). Green + high count masked the headline defect. Coverage-driven dead code (F2/F10) further inflated the count without exercising production paths.
- rule-gap (proved useful, candidates to promote): (a) Go↔Swift JSON contract — a nil Go slice marshals to `null`, and a non-optional Swift `[T]` then throws DecodingError; initialise slices to `[]T{}` (or omitempty + Swift optional/default) and add a test asserting the empty-state wire shape, dim 8/9. (b) Desktop mark-read must not clear UI state before the DB write succeeds — `try?` + unconditional `clearSectionLocally`/`result=nil` makes the UI lie on a WAL-locked write; mirror DigestViewModel do/catch and only clear on success, dim 9. (c) A new sidebar/badge ViewModel must be constructed on every path that reveals the sidebar (both `!needsOnboarding` and `completeOnboarding`), dim 6. [(a),(b),(c) promoted 2026-08-04 → review-rules "Swift / Desktop conventions"]
- outcome: TBD

## 2026-06-19 — feature/catch-up-summarizer (working-tree TCC migration) — verdict: changes-needed

- contested: SF1 severity — prosecutor (major, "no runtime guard") vs advocate (dismissed, rule-§5-sanctioned mechanism). Settled: the chosen MECHANISM is not faultable — `--setting-sources project,local` is exactly what rule §5/memory prescribe, and a rejected flag exits non-zero (loud), so this is not a CRITICAL re-open of the TCC P0. What remains is only the absence of a runtime guard, which is latent fragility, demoted to minor follow-up — NOT a blocker on this diff. The advocate was substantively right; the prosecutor's "major" overweighted a hypothetical future CLI that silently no-ops a flag it doesn't recognise. [9 error-handling / 5 codebase-fit]
- contested: F1 severity — advocate minor vs prosecutor major (test-analyzer 3/10 required-before-merge). Settled as MAJOR (changes-needed) but not a blocker: rule §6 is squarely on point (a bugfix must add a test that fails pre-fix), the entire payload of this P0-class change is unasserted in TestBuildArgs, and the fix is trivially cheap (one assertion on a static literal). Raised by 4 lanes (prosecutor + test-analyzer + codex + silent-failure) — that convergence is the signal that tipped it above minor. test-analyzer's 3/10 maps below the ≥8→blocker bar, so major, not blocker.
- false-positive: F4/SF4 DRY (`"project,local"` literal at 3 sites) — dismissed by code-simplifier + both debaters + judge: a 2-token inlined flag pair matches how every other flag literal is written; a shared constant would be inconsistent over-abstraction (rule §2). Correct call. [9 DRY]
- false-positive: SF1-as-CRITICAL and SF2/SF3 as in-scope — SF2 (nilerr fallback) and SF3 (stderr discarded on success) both verified PRE-EXISTING via git history (108edbb / 3624d79), unmodified by this diff; correctly demoted to follow-up rather than charged against the change.
- miss: F5 cwd inconsistency — the prosecutor DROPPED F5 in round 2 on a misread; only the advocate (and orchestrator re-verification) kept it alive. The diff makes cwd load-bearing for isolation (`--setting-sources` loads project/local `.claude/` relative to cwd; Generate uses ~/.config/watchtower, the others os.TempDir). Latent/pre-existing, not a blocker, but the prosecutor lane underperformed by retracting a valid finding. [8 regression]
- process: code-simplifier (declared read-only) WROTE to resolve_test.go to strip a trailing blank line (F3). Net harmless (reverted noise to HEAD, F3 moot) but a read-only reviewer performing a write is a tooling deviation worth flagging.
- weak-dimension: 7 (test quality) again — go build/vet/gofmt all clean and green, yet the one assertion that pins the entire TCC P0 payload is missing; clean+green masked the untested core (same failure mode as the prior two entries on this branch).
- rule-gap (proved useful): rule §5 (`--setting-sources project,local`, never CLAUDE_CONFIG_DIR) was decisive in resolving SF1 — the diff is exactly compliant. Candidate companion rule: a P0/TCC-class behaviour change must add an assertion that the load-bearing flag is present in the spawned argv (pins F1-class gaps), dim 6/7.
- outcome: TBD

## 2026-06-16 — feature/catch-up-summarizer (re-review, +7 fix commits) — verdict: approve

- re-review: confirming pass after `changes-needed`. All prior blockers verified CLOSED in code (not just claimed): F5 now logs both AI-rollup and unparseable-output degradation (`internal/catchup/pipeline.go:83,91`); mark-read do/catch + only-clear-on-success landed; F9 empty-slice wire shape fixed; SidebarCountsViewModel constructed on the onboarding path and its `fetch()` wraps the read in do/catch with a log. Four lanes agree (prosecutor2, codex2, silent-failure2, toolkit-reviewer2 approved). go build/vet/test/lint clean; swift build OK, 811 tests green, swiftlint --strict clean.
- contested: codex2 pipe-deadlock (CatchUpViewModel.runCLI stdout-then-stderr sequential drain, >64KB stderr could block). Verified `internal/`... actually `CatchUpViewModel.swift:269-270`: data IS read before `waitUntilExit` (the prior deadlock class), but two pipes are still drained sequentially. Settled as PRE-EXISTING shared-runner tech debt (same pattern in MeetingPrepViewModel), NOT introduced by this feature → not a blocker for this branch. Follow-up: concurrent drain in the shared CLI runner. [9 error-handling]
- follow-ups (none blocking): (1) pipe sequential-drain deadlock risk in shared CLI runner [pre-existing, dim 9]; (2) truncation UX over-promise — "Mark everything read" with truncated=true caps the snapshot and shows "all caught up" while "+N not shown" rows stay unread in DB [inherent to capping design, dim 6]; (3) internal `try?` in SidebarCountsViewModel.fetch() for inbox/recs counts silently fall back to 0 with no log [pre-existing advisory-counts pattern, dim 9]; (4) `targetsLine()` in catchup/pipeline.go:122-124 swallows GetTargetCounts error without a log — inconsistent with the F5 logging just added in the same file [minor, dim 9]; (5) N1 DigestQueries bulk `markRead` lacks the `read_at` column guard its siblings have [internal-consistency nit, forward-only schema makes it harmless, dim 8].
- false-positive (this pass): badge over-count (raw unread without max_age) — confirmed pre-existing cheap-badge choice, not a regression. [6 AC]
- weak-dimension: still 7 (test quality) on the inherent items — truncation over-promise and the SidebarCounts internal try? fallbacks remain unexercised by the green suite, but neither is a correctness blocker.
- outcome: TBD until the --outcome step records the real merge result.

## 2026-06-28 — feature/task-ai-agent (read-only MCP server, post-merge review) — verdict: changes-needed

- contested: F1 dimension+severity — prosecutor filed §9 error-handling/MAJOR, advocate argued §3/§5 consistency + dead-code/minor. Settled NOT §9: the error is surfaced LOUDLY (returned to the LLM as IsError, the opposite of §9's swallowed-error clause) and the leaked text is the stdlib sentinel `sql: no rows in result set` — no credential/PII/path, so the §9 security clause doesn't bite either. Verified in code: `GetTargetByID` (internal/db/targets.go:128-135) and `GetTrackByID` (internal/db/tracks.go:160-167) wrap `ErrNoRows` with `%w` and never return `(nil,nil)`, so the designed clean branch `if target == nil { "no target with id N" }` at internal/mcp/targets.go:49-50 is DEAD — the `err != nil` branch fires first with the raw sentinel. Contrast `GetDigestByID` (digests.go:221-223) which maps `ErrNoRows→(nil,nil)` so its clean branch lives. Real dimension = §3/§5 codebase-fit/consistency with a §6/§7 test tail; severity MAJOR (not blocker): user-to-LLM correctness defect + shipped dead code + tests give false assurance (F2 asserts only IsError==true, true on both paths). Single fix closes F1+F2 — the most worthwhile follow-up. test-analyzer 6/10 keeps it below the ≥8→blocker bar. Advocate right on dimension, prosecutor right it exceeds a nit. [3 code-style / 5 codebase-fit / 6 AC / 7 tests]
- false-positive: F6a/b (get_person name-match dropped, list_digests `since` dropped) — dismissed: the SHIPPED docs/mcp-server.md matches the impl (user_id only, no since), and FromUnix/ToUnix are pre-existing fields. Descoping aspirational spec for v1 when shipped docs match impl is a product decision, not a defect. F6c survives as real minor §2: verified JiraIssueFilter.SprintID (*int, internal/db/jira.go:794, applied :815-817) is set by ZERO callers — the sole consumer internal/mcp/jira.go:31 never sets it, no CLI/test sets the FILTER field (only the unrelated model field). The `if f.SprintID != nil` branch is unreachable; drop the field. [2 architecture]
- false-positive: F4 jira instance — prosecutor correctly WITHDREW in round 2 (TestGetJiraIssues proves exclusion at the db layer); survives only for the thin MCP digest(type/channel) + targets(status) pass-throughs as a test-hardening gap.
- miss (headline, codex-unique): C1 — list_targets silently returns [] for status=done and status=dismissed, the single strongest correctness finding on this branch, caught by NO other lane (prosecutor, all four specialists, advocate all missed it). Verified: the handler (internal/mcp/targets.go:28-34) passes Status but never sets IncludeDone, so GetTargets (internal/db/targets.go:191-196) builds `status NOT IN ('done','dismissed') AND status = ?` → for status=done/dismissed the WHERE is self-contradictory and ALWAYS EMPTY — while the arg jsonschema at targets.go:12 explicitly advertises done|dismissed as valid filter values. A documented, advertised filter value returns confidently-wrong empty results with no error. This OUTRANKS F1: F1 is a loud-but-ugly sentinel on a missing-id path, C1 is a silent wrong answer on a happy path. The codex lane returned late (after dispatch) and was the only one to find it — a recurring pattern across this project's reviews (codex carries unique correctness catches). [6 AC]
- miss: C2 (codex) — get_jira_issue returns tombstoned issues: GetJiraIssueByKey (internal/db/jira.go:252) omits the `is_deleted = 0` filter that its siblings GetJiraIssues/GetJiraIssueCount use. Minor read-model inconsistency, also codex-unique. [8 regression]
- miss: strongest coverage gaps came from pr-test-analyzer, not the debate — get_digest has ZERO behavioral tests (only handler with none; verified its sole test-file mention is the tools-list set at server_test.go:70), and list_upcoming_events — the most logic-heavy handler (default-48h, time.Now window, RFC3339 bounds) — has only an EMPTY test (people_test.go:89), no in/out-of-window data-bearing case. Both outrank the debate's own coverage points. Note: a list_targets test that exercised status=done would have caught C1 immediately — the missing exclusion/edge-value test (cf. F4) is the same blind spot. [7 tests]
- weak-dimension: 7 (test quality) AGAIN — go suite green and the harness is genuine (real go-sdk server + in-memory transport + real SQLite round-trip), yet the not-found contract tests (F2) structurally cannot fail on the leaky path, get_digest is untested, and the most logic-heavy handler has no data-bearing test. Green + a credible harness masked the untested core — the same pattern as the prior five entries.
- rule-gap (proved useful, candidate to promote): a domain getter that returns `(*T, error)` for a by-id lookup must map `sql.ErrNoRows → (nil, nil)` so callers can distinguish not-found from a real error (matches GetDigestByID / GetJiraIssueByKey / GetLatestPeopleCard); wrapping ErrNoRows with `%w` instead both leaks the stdlib sentinel and makes every downstream `if x == nil` not-found branch dead code. dim 3/5. Companion: read-only enforcement should be at the CONNECTION (open the DB `?mode=ro`), not a lexical tool-name-prefix test (F7) — that also makes the Desktop/docs "no tool can modify your data" copy literally true. dim 7/9. [ErrNoRows→(nil,nil) promoted 2026-08-04 → review-rules §5; read-only-connection companion still open]
- outcome: TBD

## 2026-06-25 — feature/catch-up-summarizer (peel-off replaces outline) — verdict: changes-needed

- contested: F1 fix sufficiency — two candidate fixes debated. Settled in favor of the ADVOCATE'S branch-level fix (only affirmative `parsed.Done` and `sectionsEmpty` set `stoppedClean`; the `parsed.Theme==nil` and `len(refs)==0` paths must `break` with `stoppedClean=false`) over the prosecutor's single-line caller gate (`stoppedClean && len(themes) > 0`). Verified against code: the caller gate closes the round-0 zero-theme case but leaves a residual — a degenerate round AFTER ≥1 theme still silently clears the (smaller) unprocessed leftover, which is the same bug class. The branch-level fix closes both because a non-`done` degenerate round is, by the design spec, NOT a clean stop. Verified `peelScript` emits explicit `{"done":true}`, so TestCatchup27 keys off real `Done==true` and survives the branch-level fix; TestCatchup28 (cap) and TestCatchup29 (syntax-error mid-loop) are on untouched paths. [6 AC / 9 error-handling]
- contested: round-0 syntax-error vs valid-degenerate JSON — TestCatchup33 already covers a round-0 *unparseable* response (correctly fails the session via `parsePeel` error). The gap is a round-0 *valid-but-degenerate* object (`{}`, `{"foo":1}`, legacy `{"themes":[...]}`): `parsePeel` returns no error (it only errors on JSON syntax), `Theme==nil/Done==false`, and line 210 treats it as a clean done → whole pool marked read with zero themes. Verified by reading `types.go:77` (`parsePeel`) + `pipeline.go:210`. This distinction is exactly why F2's new test must use VALID degenerate JSON, not garbage. [7 tests]
- false-positive: F3 (markLeftoverRead over capped pool) and F5 (O(rounds × pool) cost) — both DROPPED correctly; the capped/bounded mark-read is the intended direction and the cost is owner-accepted and bounded by maxPeelRounds=25. Not faultable. [4 efficiency]
- miss: F1 was raised by 4 lanes (prosecutor + codex×2 + silent-failure-hunter HIGH + test-analyzer gap#1) and the advocate CONCEDED — strong convergence. But the precise mechanism (valid-degenerate JSON slipping past `parsePeel` to set `stoppedClean` on a non-`done` path) needed judge code-verification to separate it from the already-covered syntax-error case; the ledger conflated the two until pinned. [6 AC]
- weak-dimension: 7 (test quality) AGAIN — this is the FOURTH consecutive entry on this branch where a green, `-race`-clean suite masked the untested core. TestCatchup27/28/29/33 fence the done/cap/error paths but NONE exercises a valid-degenerate round-0 response, which is the single most destructive path (silent irreversible mark-read of the entire backlog). The peel loop runs on the JSON-fragile light tier under a prompt that explicitly teaches `{"done":true}`, so the degenerate-shape probability is not low.
- rule-gap (proved useful, candidate to promote): when an AI-output parser tolerates any syntactically-valid JSON (unmarshal-only, no shape validation), a "stop/clean/success" decision must key off an AFFIRMATIVE positive field (`Done==true`), never off the ABSENCE of a field (`Theme==nil`) — absence collapses "model said stop" and "model emitted garbage" into one branch, and a destructive side-effect (mark-read) then fires on garbage. dim 6/9. [promoted 2026-08-04 → review-rules §6]
- outcome: TBD

## 2026-06-20 — feature/catch-up-summarizer (review-mode redesign, fix-until-clean) — verdict: approve

- re-review: the headline Round-1 BLOCKER (learning loop write-only) is verified CLOSED in code, not just claimed — `digest.LearnedPreferencesBlock` (internal/digest/preferences.go) is consumed by all four pipelines via `learnedPrefs()` (digest at 5 injection sites, tracks/briefing/catchup each at their own), each gated on `prefs != ""` and prepended. `ListLearnedRulesByPipeline` (internal/db/inbox_learned_rules.go) reads by the new `pipeline` column; inbox keeps its bare-scope lookup. The two lookup mechanisms are disjoint, which is what makes the F4 UNIQUE(rule_type,scope_key) residual actually safe. [2 architecture / 9 DRY]
- contested: F4 / P2 scope-key separation enforced by prompt only. Settled as latent-minor, not a blocker — the collision surface is real only if the AI disobeys the prefix rule (internal/catchup/prompt.go:104 instructs bare `channel:`/`sender:` for inbox, `<pipeline>:` prefix for all others), AND a non-inbox pipeline reads by `pipeline` column not scope_key, so a same-key write from two pipelines would need identical rule_type+scope_key, which the prefix prevents. Defence-in-depth (pipeline in the UNIQUE key) is a follow-up, not a gate. [2 architecture]
- contested: createTask source_type. Verified the right call — `targets` CHECK has no 'catchup' (would fail at insert, the exact codex Round-1 defect); Swift uses sourceType "manual" and preserves the theme→target link via task_id (CatchUpViewModel.swift:158-174). Reusing the existing constraint over widening it is rule §2-correct. [8 regression]
- contested: codex Round-2 scope-hint resolution — confirmed `FetchItemScopeHints` (internal/db/catchup.go:170) resolves real Slack channel_id/sender_user_id (tracks/briefings return empty by design, sql.ErrNoRows→empty best-effort), so derived keys actually match consumer keys. Without this the loop would have closed on paper but never fired. Good catch by codex; well-fixed. [6 AC]
- false-positive: idempotent acknowledge double-count — both sides now guarded (Go `alreadyReviewed`/`IncrementReviewed` at pipeline.go:350,383; Swift `wasReviewed` guard at CatchUpQueries.swift:92-101) with tests; reviewed_count cannot exceed total_themes. Dismissed as resolved. [6 AC]
- miss: nothing new this pass — codex carried the load on this branch across all rounds (createTask CHECK, GRDB cross-process refresh→poll, scope-hint id resolution were all codex-first). The prosecutor/specialist lanes mostly echoed or did style work.
- weak-dimension: 7 (test quality) again, but in a narrower way than prior entries — the suite is green AND the headline behaviour IS now tested (TestCatchup12, TestLearnedPreferencesInjectedIntoDailyRollup, briefing/tracks injection tests). The residual is breadth: of digest's 5 injection sites only daily-rollup has a wire-level behaviour test; the other 4 ride the unit-tested helper. Acceptable — same helper, same one-line call shape — but a per-site assertion would harden it. This branch broke the prior pattern (green-masking-the-core) that recurred in the three earlier entries.
- rule-gap (proved useful): the cross-pipeline "rules table shared by a `pipeline` discriminator column, consumed by pipeline OR by scope but never both for the same row" pattern is a clean way to avoid widening a UNIQUE constraint; candidate companion to rule §2. Also: a learned-rule loop must resolve table-row ids back to domain ids before forming scope keys (FetchItemScopeHints) — a derive-side/consume-side key mismatch is invisible to green tests and only codex caught it. [2 / 6]
- outcome: TBD

## 2026-07-31 — feature/signing-fix — verdict: changes-needed

- contested: F2 severity (auto-detect exactly-one gate) — debate settled minor, judge escalated to major: spec:26's unit is identities, the code counts lines; judge-reproduced dual-keychain case (count=2, sort -u→1) defeats the change's own purpose on a common config with a false diagnostic, and 3 lanes converged on it independently [ac-correctness]
- false-positive: F4 (removal of CODESIGN_IDENTITY='-' ad-hoc opt-out) — base dev mode never read the var, '-' was only an unset default, zero consumers repo-wide, and the spec explicitly removes the dev ad-hoc case; §8's back-compat condition unmet [regression]
- false-positive: F6-as-regression (UpdateService dev-build guard) — the guard's property is fail-closed signer-pinning, which Developer-ID-signed dev builds now satisfy; only comment wording went stale [regression]
- miss: explicit-identity-set-but-not-found → substitute-cert cascade was raised only by the specialist lanes (silent-failure-hunter, code-reviewer), never filed by either debater; offline-timestamp dev failure was missed by the advocate's round 1 and caught by prosecutor + codex [dry-errors-security]
- weak-dimension: the pr-review-toolkit code-reviewer lane claimed no signing spec exists under 2026-07-31-*.md although A1 of the calendar-quick-wins spec is exactly it — spec-discovery should precede its ruleset pass
- rule-gap: no rule covers build/shell-script changes (§6/§7 scope to Go/Swift tests); a rule like "a build-script behavior change must attach the spec's machine-checkable manual-verification transcript (e.g. codesign -dv) to the PR" would have made F8 mechanical; also useful precedent confirmed: `security find-identity` may list one cert per keychain — identity counts need sort -u
- outcome: TBD

## 2026-07-31 — feature/recording-event-link — verdict: changes-needed

- contested: F3 severity (codex/prosecutor major vs advocate minor) — settled MINOR: the spec names fetchForEvent verbatim, the identical sync-main-thread full-row read + silent catch is the shipped TranscriptSectionView precedent, and the CLAUDE.md perf guard's letter scopes to the master-list RecordingListItem projection, not a per-event handful of rows. Same accepted-precedent discount the prosecutor himself applied to the sibling silent-catch finding — consistency across findings in one review is part of severity calibration. [4 efficiency]
- contested: F9 (A1 half of the two-part spec absent from this branch) — ledger left it unresolved/needs-human; judge RESOLVED it in two minutes by checking sibling branches: feature/signing-fix (05c2d93, scripts/build-app.sh +32/−12) delivers exactly the spec's A1 scope. Lesson: before flagging spec partial-delivery on a multi-part spec, grep `git branch -a` / recent commits touching the other part's declared surface — the granular-branch convention means the sibling usually already exists. [6 AC]
- false-positive: F11 (selection persisting across mode switches framed as regression) — dismissed: the spec mandates the exact hoist mechanism ('RecordingsView gains an optional external selection binding'); persistence is its inherent, ephemeral consequence. A spec-mandated mechanism's direct consequence is not drift. [8 regression]
- miss: none codex-unique this round — both codex findings echoed the debate (healthy small-diff behavior). pr-test-analyzer lane-unique: the RecordingsView externalSelection??local binding fallback (the deep-link hinge AND the dominant plain-tab path) is fully unasserted — again the specialist lane carried the strongest coverage catch. [7 tests]
- weak-dimension: 7 (test quality), same bimodal shape as prior entries — query layer thoroughly pinned (both degenerate pruned-event paths), while the view branch of the flagship affordance (linked header incl. spec-mandated fallback) and the navigation plumbing shipped with zero tests; and the one badge condition whose reason-to-exist is code-commented (recap collision guard) is mutation-safe.
- rule-gap (candidates): (a) CX-1 class recurred LITERALLY one review after being codified — promote to review-rules: any new view in the recording file-family that loads recordings/recaps on appear MUST also reload on meetingRecorderCenter.phase→.idle (the sibling hook is the auto-flag), dim 6; (b) a deep-link that navigates by setting selection/expansion state must be gated on the target actually being renderable in the destination's data window — check both window edges and config-vs-hardcoded window mismatches (sync_days_ahead vs UI daysAhead), else degrade to the non-navigating affordance, dim 6; (c) a spec that enumerates two degradation states for a link (resolvable/pruned) should be attacked for the third in-between state (exists-but-not-displayed) — spec-gap findings are still P0 findings when the happy path silently no-ops. [(a) promoted 2026-08-04 → review-rules "Swift / Desktop conventions"]
- outcome: TBD

## 2026-07-31 — feature/meet-join — verdict: approve

- contested: codex's major #1 (events.list missing `conferenceDataVersion=1` → conferenceData join links allegedly never populate) — judge-dismissed on the API contract: `conferenceDataVersion` exists only on Google Calendar write operations (insert/update/patch/import), where it gates conference data in the *request body*; events.list/get have no such parameter and read responses always carry conferenceData. Code location was accurate but the third-party-API premise was wrong — this class of finding needs domain-knowledge adjudication, not code reading, and neither debater could settle it. [6 AC]
- false-positive: codex #1 above; also F7 (`http` branch in conferenceLink 'unreachable') — prosecutor withdrew on his own factual error: client.go passes conferenceData entry-point URIs through unfiltered and Google's contract permits http:. [6 AC / 2 arch-decision]
- miss: the two best user-facing catches came only from the non-debate lanes — codex + silent-failure-hunter both caught the auto-record path bypassing the SystemAudioRecorder.isSupported gate the adjacent manual Record button enforces, and silent-failure-hunter alone caught the (URL) -> Void seam structurally discarding NSWorkspace.open's Bool (recording starts for a meeting never joined). The debaters spent their depth on regex/migration/test mechanics and both missed the manual-vs-auto gate asymmetry sitting one screen away. [6 AC / 9 error-handling]
- weak-dimension: prosecutor on dim 9 — both of his dim-9 filings (F6 duplication, F7 http branch) were dropped, while the real dim-9 findings came exclusively from the specialist lanes.
- rule-gap (candidate): an auto-invoked action riding a user gesture (auto-record on Join) must inherit every capability/availability gate its manual twin enforces — a `.disabled(...)` condition on the manual button with no matching guard on the automatic path is the tell. Companion: a NEW doc comment stating an absolute concurrency guarantee ('never double-started') over a pre-existing check-then-act guard converts an inherited latent race into a false written contract — either land the one-line enforcement or write 'best-effort'. dim 6/9. [auto-vs-manual gate rule promoted 2026-08-04 → review-rules "Swift / Desktop conventions"]
- outcome: TBD

## 2026-07-31 — feature/informative-calendar (vs feature/meet-join) — verdict: changes-needed

- contested: codex's zero-minutes stop-push 'major' — judge settled it as spec-faithful behavior (spec §2 scopes reminderMinutes/'0 disables' to the pre-meeting push and banner; §3's stop push is minutes-independent by design); residual = a factually wrong class doc comment (':101 0 disables all reminder surfaces') and a product-scoping question merged into the F5 owner item. Lesson: when a lane's finding hinges on which setting SHOULD scope a surface, adjudicate against the spec's section structure before accepting the severity. [6 ac-correctness]
- contested: F4/F9 severity — debate settled both at minor (agreed), judge elevated to one consolidated MAJOR: silent-failure-hunter supplied evidence the debate never weighed (poll()'s ?? [] clears a visible banner on transient error; pollStopReminder's try? makes the privacy-relevant safety push persistently invisibly dead), and the shape matches the 2026-07-15 F5 precedent (a NEW component's entire diagnostic layer silent against the sibling convention its own doc cites — DigestWatcher logs). An agreed debate disposition is overridable when a specialist lane adds mechanism evidence, not just severity re-assertion. [9 error-handling]
- false-positive: F6 (per-day loop 'unindexed scans' — idx_calendar_events_start/end exist, pre-existing shape); F8 (test hermeticity via ConfigService() — assertions invariant to the config value, established house pattern); F12/F14 (unbounded dedup sets / minutes=0 poll cost — negligible by construction). All correctly dropped in-debate; F14's clarity core survived via code-simplifier's re-framing as a gating-documentation nit. [4/7]
- miss: F1 (backfill -> inbox calendar_invite flood) was prosecutor-ONLY — all four specialists and codex missed the cross-module consumer of the widened sync window; the spec's own 'no cleanup change needed' reasoned only over calendar tables. The strongest finding this run came from adversarial cross-module tracing, not coverage/simplification lenses. [8 regression]
- weak-dimension: codex lane (one finding, adjudicated spec-faithful) — historically this repo's unique-correctness lane, thin here; also the recurring dim-7 bimodal pattern (superb pure-logic suite, zero coverage at the NotificationDelegate/notification-action seam — same highest-blast-radius-thinnest-coverage shape as prior entries).
- rule-gap (candidate, promote): widening a sync/backfill time window (or any watermark lookback) requires auditing every downstream consumer that selects on synced_at/updated_at/watermark WITHOUT its own event-time bound — the backfill inherits fresh sync timestamps and replays as 'new' into detectors; the tell is a detector query with no start/end-time predicate. dim 8. Companion (proved useful again): a new poll-loop service must log its poll errors like the sibling it cites (2026-07-15 (b) recurrence, second occurrence — promote). [both promoted 2026-08-04 → review-rules §8/§9]
- outcome: TBD

## 2026-07-31 — feature/transcript-segments — verdict: changes-needed

- contested: F3/silent-failure Finding 1 severity spread (debate settled nit; silent-failure-hunter filed HIGH) — settled MINOR: the drop path is provably unreachable from the only production caller (Swift text IS render(utterances), renderers fixture-pinned), so HIGH overweighted it, but nit underweighted that the render-mismatch branch is the designed tripwire for this repo's known dual-path drift class and fires invisibly (stderr discarded on exit 0, no segments_ok in an envelope that already carries recap_ok). Invisible-degradation lesson (2026-06-16) applies to the tripwire itself, not just today's reachability. [9 error-handling]
- contested: F2 (segments temp-write aborts save) — silent-failure-hunter re-raised MEDIUM after the prosecutor had already dropped it; judge upheld the dismissal: the proposed degrade-to-segmentless-save would let removePersistedTranscript delete the sidecar and permanently NULL recoverable segments on a transient tmp error, while shipped fail-loud + sidecar retry recovers everything (test-pinned). Specialist lanes re-litigating a debate-settled item without the retry-path evidence is a known noise mode. [9]
- false-positive: F9 (encode swallow in setUtteranceDeleted) correctly dismissed by type-level proof (JSON-round-tripped finite doubles cannot fail JSONEncoder) — BUT the same guard shape in TranscriptSaveService takes in-memory never-round-tripped utterances, where the proof does not apply. Lesson: a 'provably dead branch' dismissal is per-call-site, not per-function-shape; check every twin site before generalizing the dismissal. [9]
- miss (codex-unique, again): stale chatVM after utterance edit (RecordingDetailView.swift:208-220 vs let-snapshot at MeetingChatViewModel:22) — the branch's only genuine runtime correctness bug, caught by NO debate lane and NO specialist; judge-verified and ruled major. Recurring pattern (4th entry): codex carries the unique silent-staleness/wrong-context catches on this repo; debate lanes anchor on the diff's own new files and miss cross-file consumers of newly-mutable state. [6 AC]
- weak-dimension: none catastrophic this run — test quality was the branch's strength (8.5/10, degenerate branches covered both sides); the residual gaps (live-path assertion, sidecar back-compat) were caught by 2 lanes each. The debate itself performed well: 8 of 12 prosecution findings dismissed with evidence, two of them because the proposed fix was strictly worse than shipped behavior (F2, F7) — severity calibration via reading the actual retry/consumer path, not the abstract bug description.
- rule-gap (candidates): (a) when a view gains the ability to mutate data that a lazily-created sibling ViewModel snapshotted at init, every mutation site must invalidate or recreate that VM — newly-mutable state + let-snapshot consumers is the auto-flag (chatVM class, same shape as 2026-07-13 CX-1), dim 6; (b) a branch stacking a goose migration above numbers claimed by unmerged sibling branches must carry an explicit merge-order/renumber note in the PR body — the spec's 'renumber at merge time' clause has no enforcing mechanism, dim 8; (c) an envelope that already reports per-feature degradation (recap_ok) should gain the matching field for each NEW best-effort payload in the same command — asymmetric envelopes are the tell, dim 9. [(a),(c) promoted 2026-08-04 → review-rules "Swift / Desktop conventions"]
- outcome: TBD

## 2026-07-31 — feature/meeting-chapters (vs feature/transcript-segments) — verdict: changes-needed

- contested: F2 severity — debate settled 'minor with mandatory fix' but I elevated the conversion cluster to MAJOR by merging four lanes hitting it from different angles (prosecutor F2+F7 stamp-loss + encode-fail return-in-transaction, silent-failure #2 orphan-Target/no-idempotency-guard, test-analyzer #2 [6] View-layer untestability, code-reviewer double-click advisory). The 2026-06-19 precedent held: multi-lane convergence on a cheap fix is the tipping signal even when each fragment alone reads minor. [9 error-handling / 6 AC]
- false-positive: F3 (migration numbering) — prosecutor's premise '00044/00046 exist nowhere' was factually wrong; git ls-tree across branches showed both allocated on parallel same-day worktrees, and the proposed fix would itself have caused the goose collision it cited. Lesson: parallel-worktree migration claims must be checked across ALL branches, not the current line. F4 (timecode bounds) also dismissed on arithmetic — floor-monotonicity of FormatTimecode makes the claimed trigger impossible; validation asymmetry was deliberate fail-closed design. [8 regression / 6 AC]
- miss: silent-failure-hunter uniquely caught that Swift never consumes the new chapters_ok/chapters_error envelope keys (TranscriptSaveResult unchanged, MeetingRecorderCenter:491 keys off recapOK only) — neither debater noticed the envelope's Swift consumer was left behind. Pattern: when Go adds additive envelope keys per spec, verify a Desktop consumer exists or a documented reason it doesn't. [9 error-handling]
- weak-dimension: codex lane — timed out twice (300s hard limit, exit 124), zero output. The lessons log shows codex repeatedly carrying unique correctness catches; this review ran without it, so residual-risk confidence is lower than the lane count suggests.
- rule-gap (candidates to promote): (a) a write transaction pairing a create with a stamp must live in the query layer and THROW on any stamp failure so rollback covers both — a silent no-op stamp beside a committing create is the partial-success class even when each half is individually precedented (dim 9); (b) a regenerable AI artifact that carries user-authored state (conversion stamps) needs either carry-over or a destructive-action confirmation before wholesale overwrite (dim 6); (c) additive envelope keys added in Go must gain a Swift decoder field in the same change (dim 8). [(c) promoted 2026-08-04 → review-rules "Swift / Desktop conventions"]
- outcome: TBD

## 2026-07-31 — feature/speaker-identity (vs feature/transcript-segments) — verdict: changes-needed

- contested: F6 (GetCalendarEventByID err==nil && ev!=nil conflation) — debate dismissed on a judge-verified 5-site pre-existing package precedent while silent-failure-hunter independently rated the same site MEDIUM-HIGH as its top finding. Settled MINOR: accepted in-codebase convention + ephemeral user-confirmed artifact keeps it off the gate, but the (false,nil)-vs-(false,err) rule-gap class (2026-07-15 candidate c) recurred for the fourth time — promote candidate. [9 error-handling]
- false-positive: codex's sole finding — a claimed Swift compile error on `zip(a,b).map { $0 * n + $1 }` (VoicePrintMatcher.swift:50); tuple-splat for shorthand closure args typechecks fine, reproduced exit 0. Codex lane contributed ZERO net findings this run — its first fully-empty pass on this repo, breaking the \"codex carries unique correctness catches\" streak. [6 AC]
- miss (lane-unique, folded into verdict): silent-failure-hunter alone caught that every loadTranscriptSpeakers stderr degradation warning is discarded by ProcessCLIRunner on the exit-0 path — the only diagnostic for the F1 major is invisible exactly where it fires (envelope speakers_ok flag is the fix shape, recap_ok precedent); it also alone caught renameSpeaker's Void return silently consuming a confirm chip on stale state. [9]
- weak-dimension: 7 (test quality) again, in the now-standard bimodal shape — library layer thoroughly tested (exact-threshold cosine, centroid cancellation, degenerate-input matrix) while the new @Observable center shipped with zero tests despite its sibling suite existing on the same branch, and the feature's AI half (event-linked prompt path) had no test at all (every test used eventID:\"\").
- rule-gap (candidates): (a) an LLM-proposed identifier feeding a destructive/persistent write must be validated against the RESERVED/sentinel label space («Я», Speaker N), not only against the known-target set — validating membership in the eligible set while ignoring collision with privileged labels let one click corrupt the cross-recording identity loop, dim 6/9; (b) a CLI degradation warning must survive the production caller's exit-0 path — if the caller only surfaces stderr on failure, partial-degradation must ride the success envelope as a flag, dim 9; (c) fourth recurrence of the existence-check (false,nil)-vs-(false,err) class — promote to review-rules §9. [(b) promoted 2026-08-04 → review-rules "Swift / Desktop conventions" (envelope rule); (c) promoted 2026-08-04 → review-rules §9; (a) still open]
- outcome: TBD

## 2026-08-03 — feature/slack-multi-account (PR #58, vs Google multi-account precedent PR #48) — verdict: changes-needed

- contested: F3 severity (both debaters deferred to judge) — settled MAJOR, not blocker: `syncInboxReactions`/`syncChannelReadState` (internal/sync/orchestrator.go) run account-unscoped DB queries (`GetInboxItems`, `UnreadDigestChannelIDs`/`ChannelIDsWithoutLastRead`) and then call THIS orchestrator's own (single-account) Slack client against a de-namespaced raw id drawn from ANY connected account. Judge-verified the actual failure mode: absent a genuine raw-Slack-ID collision across two real orgs, the foreign-account attempt fails loud (`channel_not_found`/similar → logged warning → `continue`) — guaranteed to fire every cycle with >1 account but self-healing, not silently wrong. The corruption case (writing account B's read-state under account B's key using account A's fetched data) requires an actual cross-org raw-id collision, which — unlike the 2026-07-26 IMAP UIDVALIDITY case (an RFC-3501-*guaranteed* reuse scenario) — has no protocol-level certainty for Slack's ID keyspace. Real bug, zero test coverage, 3-lane convergence (prosecutor + code-reviewer expansion + test-analyzer's coverage-gap framing), but the corruption path is possible-not-certain where the promoted-to-blocker precedent required a documented protocol guarantee. Mandatory same-batch fix, not a deferrable follow-up. [6 AC / 8 regression]
- contested: F1 severity (both debaters converged MAJOR pre-judge) — AFFIRMED MAJOR, judge did not override, but elevated its fix scope: `SetSlackAccountAuthState` is genuinely never called on sync failure (internal/daemon/daemon.go, cmd/sync.go), matching the 2026-07-26 IMAP precedent's "inherited shape, not novel" discount. What's new this cycle: CLAUDE.md's own Slack Multi-Account section AFFIRMATIVELY ASSERTS the false invariant verbatim ("one account's auth failure records only *its* status/error... via SetSlackAccountAuthState and never blocks the others") — a documented-false contract, not merely an omission. Judge kept MAJOR (isolation itself is real and tested; only the status write-back is missing, and the failure is not silently swallowed — it's logged and surfaces in last_sync.json) but the fix batch must correct the status-write gap in code, which auto-repairs the doc claim rather than requiring a separate doc-only fix. [6 AC / 9 error-handling]
- miss (codex-unique, twice, both confirmed real by independent reproduction): (a) `cmd/slack.go`'s `slack add`/`slack login` never register a `--app-return` flag (every sibling — google/gmail/calendar/outlook — does), while Desktop's `SlackAccountsViewModel` unconditionally passes it; cobra rejects unknown flags before RunE runs, so every Desktop "Add Slack Account"/"Re-login" click fails immediately (exit 1) — the entire feature's primary entry point is dead on arrival. Judge-verified directly (`slackAddCmd.Flags()` has only `--label`/`--no-open`; grep across Swift confirms `--app-return` is hardcoded in every call site). (b) migration 00048 rewrites `messages.channel_id`/`user_id` but not `text`/`is_deleted`, so the FTS-sync trigger (`AFTER UPDATE OF text, is_deleted`) never fires and `messages_fts.channel_id` stays stale — `SearchMessages`'s equi-join on `channel_id` then returns zero rows for every pre-upgrade message, permanently breaking search on every existing install. Both are the two blockers of this review; codex is again the lane that caught the release-breaking bugs no debate-side or specialist lane found, continuing this repo's established pattern. [6 AC]
- false-positive: none dismissed this pass — every prosecutor/specialist finding survived at some severity; the debate's own quality (advocate conceding all 8 prosecutor findings plus both codex blockers in round 2) left nothing to overturn.
- weak-dimension: 7 (test quality) in a new specific shape — the migration test suite is "exemplary" per pr-test-analyzer (it already caught and pinned an unrelated off-by-one), yet the exact FTS-trigger-column omission (codex-B) sat right next to that well-tested migration and was invisible to it because no test ever asserted post-migration `messages_fts` searchability, only row-count/id-shape equality. A migration test suite can be thorough on ITS OWN stated invariants and still miss a downstream trigger dependency it never asserts against. Candidate rule: a migration that UPDATEs a column covered by an `AFTER UPDATE OF <cols>` trigger on the SAME table must either touch a covered column or explicitly re-sync the trigger's side effect (rebuild the FTS row) in the same migration, plus a test that queries through the dependent index/view post-migration, not just the base table.
- rule-gap (candidates): (a) above (migration + partial-column-list trigger interaction) — promote; (b) a Desktop ViewModel that hardcodes a CLI flag (e.g. always passes `--app-return`) needs a corresponding `init()` registration audit across ALL of that command's RunE-bound cobra commands, not just a "matches the sibling pattern" doc claim — this is mechanically greppable (`grep app-return cmd/*.go WatchtowerDesktop/**/*.swift`) and would have caught codex-A in seconds; worth a pre-flight grep step in the review process itself, not just a rule; (c) an account-scoped daemon phase that fans out one syncer per account must have EVERY DB query inside that syncer's call graph scoped to its own account id — an unscoped list-then-strip-namespace query is the tell (F3 class), dim 6/8. [(b) promoted 2026-08-04 → review-rules "Swift / Desktop conventions"; (a) promoted 2026-08-04 → review-rules §8; (c) still open]
- outcome: TBD

## 2026-08-03 — fix/transcriber-speaker-fixes (capture/processing decoupling + diarization fixes) — verdict: changes-needed

- contested: F4 severity (advocate minor / prosecutor major, the debate's only unresolved item) — settled MINOR-with-mandatory-fix on a **residency-duration** axis applied consistently across the whole review: `stopAndProcess`'s error path (MeetingRecorderCenter.swift:597-599) releases the engine slot after `liveTask?.cancel()` without awaiting the orphaned task, so a parked job can load a second engine while the cancelled one still grinds through its in-progress `transcribeWindow` — but that double residency is *bounded* by one window, whereas X2 (a failed live-path job retaining `job.transcriber` until manual Dismiss) is *indefinite*. Same broken invariant (the file's own new doc at :22, "Exactly one transcription engine is ever resident"), two very different exposures; rating both major would flatten the distinction the fix priority depends on. The prosecutor was right on the *mechanism* and that half is judge-affirmed: reachability genuinely changed from user-initiated (main gated `startRecording` on `!isBusy`, so nothing could claim the slot automatically) to automatic (`acquireEngineSlot`'s `while isCapturing || liveTask != nil` sees `liveTask == nil` immediately). "Inherited verbatim from main" was a false defence — the *line* is inherited, the *consumer that makes it fire* is new. [4 efficiency / 2 arch-decision]
- contested (judge-resolved; neither debater nor specialist had the whole picture): the corrupt-`.meta`-sidecar question. `scanRecoverable` (:1053-1058) `compactMap`s away BOTH the missing-sidecar case (correct — the sidecar is removed on successful save, so absent = already saved, and a test pins it) and the present-but-undecodable case (wrong — the sidecar's own doc at :1024 defines *presence* as "this recording was never saved", so a corrupt one means the recording IS unsaved and is now silently unrecoverable, then swept by the Go orphan cleanup). Ruled: fix the corrupt case only — degrade to `RecoverableRecording(eventID: nil, title: nil)` — which does not touch the missing-file contract the debate pinned. FIFTH recurrence of the promoted-candidate class from 2026-07-15(c)/2026-07-31 speaker-identity(c): conflating "datum absent" with "read/decode failed" at a validation site. The variant is new (absent-vs-corrupt rather than `(false,nil)`-vs-`(false,err)`) and the whole `.meta` mechanism is branch-new, not inherited. [9 error-handling]
- contested: both of the general-reviewer's blockers downgraded to major, each on an existing log precedent rather than judge intuition. (a) F1 (the legacy `phase` projection never reaching `.idle` while any job lingers, so RecordingsView/EventRecordingsSection/TranscriptSectionView never reload) — the transcript is durably saved and any navigation shows it through the sibling `.onAppear`, so it is stale UI, not the silent-permanent-loss shape this log reserves for blocker. (b) X1 (`rec_<yyyyMMdd_HHmmss>.caf` collision, now concurrently reachable) — permanent audio loss, but the trigger needs two starts inside one wall-clock second, i.e. *possible-not-certain*: the exact axis on which 2026-08-03 F3 (Slack raw-id collision) was kept at major while 2026-07-26's RFC-3501-*guaranteed* UID reuse was promoted to blocker. Zero blockers with four majors reads odd but is the calibrated call. [6 AC / 8 regression]
- pattern (the review's most useful synthesis; no single lane had it): THREE separately-filed findings share one design decision — a failed job stays in the `jobs` array until manual Dismiss. That single fact produces F1 (a lingering failure pins `phase` off `.idle`), X2 (its `transcriber` stays resident, 1.5-3 GB), silent-hunter M3 (a stale failed pill for an audioURL already re-transcribed lets a later Retry write a duplicate transcript row) and the `visibleJobs.prefix(3)` overflow hiding the RUNNING job behind stale failed pills. Patching each symptom independently (the ledger's own fix directions) leaves the shape intact. Lesson: when ≥3 findings across ≥3 lanes terminate at the same lifetime/ownership decision, the judge should name the decision, not just merge the findings — the fix batch is otherwise sized to the symptoms.
- false-positive: F6 (unvalidated threshold TextField — every neighbouring Advanced field is equally unvalidated and the NEW field is the only one carrying a range check, TranscriptionEngine.swift:91; prosecutor verified and withdrew), F8 (queue starves while recording — the priority IS documented with rationale at :22-28, deliberate and data-safe), F9 (4th cleanJSON copy — pre-existing on main and genuinely un-unifiable: array-vs-brace payload slicing differs), F12 (test-fixture inheritance — 3 setUp lines beat the abstraction), F15 (fixed yield-count synchronization — house idiom at 6 pre-existing sites). Note the F6 shape: a change that adds the *first* validation in a block is not faultable for the block's pre-existing laxity; whole-block clamping is a follow-up. [3 / 4 / 9]
- miss (lane-unique, both folded into the verdict): codex again carried the release-risk catch (X1's full consequence chain — B overwrites A's audio, then A's successful save removes the *shared* sidecar and persisted transcript, leaving B unrecoverable and Go-swept) plus the structural reason no test could have caught it (fake recorders return preset distinct URLs, so `timestampComponent` never runs under test). silent-failure-hunter alone caught M1 (disabled Retry/Dismiss `.help()` strings state a reason that can be false — the recoverable/captureError preconditions go unmentioned) and M3. Apart from F1, neither debater traced a consumer *outside* MeetingRecorderCenter.swift; the diff's own new files absorbed both lanes' depth again.
- weak-dimension: 7 (test quality), the familiar bimodal shape with a sharper tell than usual: the well-tested new behaviours are the *pure* ones (RoleAssigner's 120 s cap has both a split test and an exactly-at-cap boundary test; `filterMegaClusters` is a static pure function), while the queue's failure/recovery machinery — `dismissFailure`, `captureError`, engine-slot release on a FAILED start (regression = queue deadlock) — shipped with zero tests, and the branch's headline invariant ("the job carries its own eventID/title so recording A never files under meeting B") is unasserted: MeetingRecorderQueueTests asserts only `runner.invocations.count`, never `--event-id` per invocation, so reverting `save()` to `currentEventID` keeps the suite green while silently misfiling transcripts. Purity attracts the tests; lifetime/ownership state does not.
- rule-gap (candidates): (a) promote the generalized absent-vs-unreadable rule (5th recurrence): a scan/validation site that treats "file missing" as a meaningful *contract state* must handle "file present but undecodable" as a THIRD state and never fold it into the missing branch — the tell is a `compactMap` over two chained `try?`s where only one of them means "absent", dim 9; (b) a wall-clock-second filename mint stops being safe the moment a mutual-exclusion guard widens (here `!isBusy` → `isCapturing`) — widening ANY such guard requires re-auditing every name/key minted at second resolution inside the newly-concurrent region, dim 8; (c) a legacy single-slot projection kept as a compatibility shim (`var phase`) must be audited against its consumers' *predicates*, not just its cases — three views keying off `.idle` turned an added terminal state into a silent no-reload, and the projection's own comment ("until the queue UI lands") went stale in the very branch that landed the queue UI, dim 8; (d) the 2026-07-31 promoted rule ("a new recording-family view must reload on phase→.idle") needs its converse — a change to the `phase` projection must prove `.idle` is still REACHABLE from every terminal state, dim 6/8. [(a),(c),(d) promoted 2026-08-04 → review-rules "Swift / Desktop conventions"; (b) still open]
- outcome: TBD

## 2026-08-04 — fix/transcriber-speaker-fixes (round 2, after 21836006 applied the round-1 findings) — verdict: changes-needed

- contested: the engine-slot residual's severity (reviewer/prosecutor major vs the round-1 precedent's minor) — settled **MAJOR**, deliberately diverging from round 1's own ruling on the *same broken invariant*. Round 1 discounted it on a residency-*duration* axis (bounded by one `transcribeWindow`); that axis still holds, but two things changed: the round-1 fix (`MeetingRecorderCenter.swift:614-632`) now carries a comment asserting the exact mechanism it fails to establish ("hand the slot on only once it has actually exited, or a parked job would load a second engine alongside it") while `liveTask = nil`/`loadedTranscriber = nil` still run EAGERLY, before the `await orphan?.value` — and the failed job it appends leaves `isBusy == false`, so `retriableFailureID` is non-nil and one **enabled** Retry click (no race) walks straight through `acquireEngineSlot`'s `while isCapturing || liveTask != nil`. The 2026-08-03 Slack F1 precedent governs: a documented-false invariant is worse than an undocumented gap, because the next reader trusts the comment. Deferring only the *wake-up* while clearing the *gate variable* eagerly restores wake ORDERING but not MUTUAL EXCLUSION — these are separate properties and the fix addressed one. [2 arch-decision / 9]
- false-positive (framing, not mechanism): the prosecutor's "first-fence-wins is a REGRESSION vs main" for `internal/meeting/cleanJSON`. Reproduced both versions on the disputed input: main only trimmed EDGES (`TrimPrefix("```json")`/`TrimPrefix("```")`/`TrimSuffix("```")`), so a response opening with prose ("Here is an excerpt:") never matched a prefix and main returned the whole blob — **main fails that input too**. The real defect is divergence from the `internal/tracks` precedent the new comment explicitly CITES (tracks prefers `` ```json ``, which parses it). Lesson: a "regression vs main" claim must be reproduced against main's actual function — a branch can be wrong AND not a regression, and that distinction is exactly what sets severity. Contrast the sibling defect, which IS a true regression: `{"notes": "use ``` to fence"}` inside a fenced block parses on main and is TRUNCATED on the branch by the leftover closing-fence cut at `pipeline.go:531-533`. (Re-reproduced in round 3 after this label was disputed — see that block. Precise boundary: with the closing fence present, main parses and the branch truncates → regression; add trailing prose after the closing fence and BOTH fail, main by returning the payload whole, the branch by truncating. Truncation is structurally impossible on main: `TrimSuffix` removes at most one trailing fence and cannot cut mid-payload — a mid-payload cut needs the `strings.Index` the branch introduced.) [8 regression / 6 AC]
- miss (self-inflicted, the run's most transferable lesson): **the fix commit's own new tests pin the residual defects.** `pipeline_test.go` added `{"```\n{\"first\": true}\n```\n```json\n{\"second\": true}\n```" → {"first": true}` with the comment "First fence wins" — codifying the inversion as intent — and added the backtick-in-string case only on the BARE path (`{"key": "a ``` b"}`), never the fenced variant that breaks, so the suite documents the intent while missing the path that violates it. Symmetrically, `testOrphanedEngineLoadCannotClobberTheNewLivePass` (`MeetingRecorderQueueTests.swift:903`) waits for `liveEngineState == .unavailable`, i.e. it *requires* engine #2 to load during the orphan's residency; after the correct fix that recording parks in `.waiting` and the test hangs its `waitUntil`. Both need conscious rewriting, and neither is a weakening (the new assertions are strictly stronger: `engineLoads == 1`, state `.waiting`), so no owner sign-off is needed — but a round-N review must explicitly ask whether round-(N-1)'s NEW tests pin the residual, or the next fixer "patches" the test toward the bug. [7 test quality]
- weak-dimension: none, and worth recording as such — this is the log's first round-2 pass and the delta was strong (test-analyzer 9/10, all five round-1 majors genuinely closed with tests that fail pre-fix: `savedTick` pinned at bump time, `uniqueRecordingURL` unit+e2e, engine retention on both clear sites, per-job `--event-id` asserted at 3 sites, the whole failure/recovery machinery that round 1 called out as untested). The residual list is 2 majors + 5 minors on a 3000-line diff. What survived is concentrated in exactly the two places where a fix wrote a NEW invariant claim (the center's engine-slot comment, cleanJSON's doc paragraph) rather than in the mechanics.
- also settled: the doc split (advocate said CLAUDE.md is true and the source header overpromises) — verified the reverse. `MeetingRecorderCenter.swift:22-24` says "a queued job never **starts** while a recording is capturing" (accurate); CLAUDE.md's new Capture/processing bullet says the queue "never **runs** a job while capture is active" and then, in the same sentence, describes a recording that starts mid-job parking its live pass until the slot frees — i.e. the job demonstrably keeps running during capture. CLAUDE.md is the text that changes. The tell for this class: a "never X" clause followed by a "meanwhile Y continues" clause about the same mechanism. [3 code style]
- rejected as re-litigation: the yield-count → `ContinuousClock` deadline conversion for the new `waitUntil` helper (round 1 dismissed this class as a house idiom at 6 pre-existing sites, F15). Kept as an optional nit, explicitly outside the mandatory batch. A round-2 lane re-raising a round-1 false-positive against a NEW instance of the same idiom does not reopen it absent new evidence (recurrence of the 2026-07-31 "specialist re-litigating a settled item" noise mode, now visible round-over-round rather than lane-over-lane).
- rule-gap (candidates): (a) promote — when a fix adds a comment/doc asserting an invariant, every gate variable that invariant READS must be checked against the fix, not just the code path that was reported; eagerly clearing a mutual-exclusion flag while deferring only its wake-up is the recurring shape, dim 9/2; (b) a "regression vs main" finding must be reproduced by running main's version on the same input before severity is assigned, dim 8; (c) round-N review checklist item: diff round-(N-1)'s ADDED tests for assertions that encode the residual defect as intent, dim 7. [(a),(b),(c) promoted 2026-08-04 → review-rules "Review process"]
- outcome: TBD

## 2026-08-04 — fix/transcriber-speaker-fixes (round 3, after 9299db9a + 4de88ffe) — verdict: approve

- outcome of the arc: three rounds, findings converging monotonically — round 1: 5 majors; round 2: 2 majors + 5 minors; round 3: 4 minors, all fixed in the same batch that reported them; round 3 close: **zero remaining accepted findings**. Judge-verified the two round-2 majors at their sources rather than on lane report: `MeetingRecorderCenter.swift:638-661` now keeps `liveTask` set as the engine-slot claim and clears it (plus `loadedTranscriber`) INSIDE the deferred `Task` after `await orphan?.value`, with `liveGeneration += 1` still eager and a comment that finally describes the mechanism it implements; `internal/meeting/cleanJSON` restored the `` ```json ``-wins precedence, dropped the closing-fence cut, and its test table now pins the fenced-backtick case *and* the fenced-backtick-plus-trailing-prose case (`pipeline_test.go:146-148`) where round 2 had only the bare-path variant. `go test ./internal/meeting/ -run TestCleanJSON` green.
- **the correction that wasn't (the run's most important lesson):** the orchestrator relayed a reviewer reproduction asserting that main's `cleanJSON` truncates the fenced-backtick input identically, therefore B1 was never a regression and my round-2 block should be retracted. I re-ran main's exact function (`main:internal/meeting/pipeline.go:512-518`) on that input: **main parses it, payload intact.** Truncation is *structurally impossible* on main — `TrimSuffix` removes at most one trailing fence and cannot cut mid-payload; a mid-payload cut requires the `strings.Index(s, "```")` the branch introduced. The reviewer's repro almost certainly used the trailing-prose variant, where both sides fail — but "both fail" is not "both fail the same way": main returns the payload WHOLE (the caller reports it raw, which is the documented contract) while the branch returned `{"notes": "use`, so even the shared-failure variant differs in the quality of the error the operator sees. The round-2 label stands; I refined it in place with the variant boundary instead of retracting it. My own round-2 rule-gap (b) ("a regression-vs-main claim must be reproduced against main's actual function") applies symmetrically to a RETRACTION request — a relayed correction is a claim like any other, and the judge owning this file is exactly what keeps a reproduced finding from being overwritten by an unreproduced one. [8 regression]
- miss (codex-unique, again — now the established pattern on this repo): `migrateLegacyPendingDefaults` cleared the three durable `recorder.pending*` `UserDefaults` keys without writing a `.meta` sidecar, so a user who relaunched, saw the recovered pill and quit before acting on it lost the recording entirely — the in-memory entry died with the process and the Go orphan sweep later reclaimed the audio. Note round 1 had explicitly REASONED about this exact function and concluded the omission was safe ("the only window it would cover is a crash between this launch and the user acting on the pill") — the reasoning was right about the window and wrong about it being negligible, since quitting the app is not a crash and is the ordinary case. Fixed at `:818-830` (sidecar first, then clear, and only when the file still exists). A documented deliberate omission is not evidence of correctness; it is a claim to re-test when the surrounding lifetime model changes. [6 AC / 8 regression]
- **pattern promoted from candidate to standing rule (recurred both rounds):** the fix commit's own new tests pinned the residual defect, twice in round 2 (the `"First fence wins"` case codifying the inversion as intent; `testOrphanedEngineLoadCannotClobberTheNewLivePass` requiring engine #2 to load) and twice more in round 3 (a test docstring naming `failJob` as the releasing site when it is not; the legacy-migration test that never modelled a genuine relaunch with a fresh Center, which is why codex's data-loss path stayed invisible to it). Round-N review MUST diff round-(N-1)'s ADDED tests for assertions that encode the residual as intent — otherwise the next fixer patches the test toward the bug. Round 3 got this right: the rewritten orphan test is verified to fail on pre-fix code.
- weak-dimension: none. Worth recording as the counter-example the log lacked: the dimension that carried this arc was test quality (delta 9/10, rewrites strictly stronger, prosecutor and reviewer independently running 45/45 before filing all-clear) — the same dimension flagged weak in five consecutive prior entries. The difference was that round 2 named *which* tests had to be rewritten and why, rather than asking for "more coverage".
- deferred, recorded, non-blocking: `internal/tracks` carries the same fenced-truncation shape (out of scope for this branch — but it is now the only remaining copy, so the next tracks change should take it); the `liveSamples` termination contract became load-bearing for the slot fix (documented, and the fake mirrors it — a seam whose fidelity is now a correctness dependency, not just a convenience); the `` ```json ``-tag trim stays greedy on `jsonc`/`jsonl` (pre-existing, both copies).
- rule-gap: rule-gap (b) from the round-2 block generalizes — reproduce before you relabel, in EITHER direction. Both a "this is a regression" and a "that was never a regression" claim are assertions about a specific function on a specific input, and the cost of accepting an unreproduced retraction is a permanently wrong lesson in the file every later review reads.
## 2026-08-04 — feature/slack-multi-account (PR #58, round 1) — verdict: changes-needed

- contested: severity of the two headline findings — no debate needed, both were clean blocker calls on first pass: (1) `--app-return` never registered as a flag on `slack add`/`slack login` (cmd/slack.go), so every Desktop "Add Slack Account"/re-login click round-trips through a CLI flow that silently drops the app-return redirect — the flagship connect affordance breaks for 100% of users; (2) migration 00048's scalar-id `"1:"` rewrite never touched `messages_fts` (a standalone, non-external-content FTS5 table with its own id columns, populated only by an INSERT/text-UPDATE trigger that an id-only rewrite never fires), so `messages.channel_id = messages_fts.channel_id` stops matching for every message synced before the upgrade — full-text search over all pre-migration history silently returns nothing. [6 AC]
- contested: 3 majors, all judge-verified real on first pass: `SetSlackAccountAuthState` was written but never called on a sync failure (an account could stay "ok" forever after its token died); migration 00048's `sender:`-scoped learned-rule rewrite matched ANY `sender:%` row, silently corrupting Gmail (raw email) and Jira (issue-key) learned mute/boost rules on upgrade by re-namespacing them as if they were Slack ids; `syncInboxReactions`/`syncChannelReadState` queried account-unscoped helpers (`GetInboxItems`, `UnreadDigestChannelIDs`) and handed the raw ids straight to a single-account Slack client with no ownership filter — a raw-id collision across two connected workspaces could write into the wrong account. [9 error-handling / regression]
- false-positive: none this round — all findings converged cleanly across lanes with no adjudication needed.
- miss: none — codex and the debate lanes converged on the same two blockers independently; no lane-unique catches this round.
- weak-dimension: none flagged distinctly this round.
- rule-gap (candidates): (a) a CLI flag added to one subcommand for a shared OAuth flow (`--app-return`) must be registered on EVERY subcommand that reaches that flow, not just the one the author was testing — the missing-flag class is invisible to `go build`/`go vet` and only shows up as a silently-ignored bool default, dim 6; (b) a scalar-id-rewrite migration touching `messages`/`channels`/etc. must also rewrite any STANDALONE FTS5 shadow table with its own copy of those columns — trigger-populated FTS tables do not participate in a plain `UPDATE ... SET id-column` unless the trigger's own condition (text/is_deleted change) fires, dim 8; (c) a bare-prefix rewrite (`sender:%`) applied identically to a multi-source table must exclude every OTHER source's id shape by its OWN shape signature (contains '@', contains '-', literal sentinel) when no source-prefix already discriminates it, dim 8. [(a) promoted 2026-08-04 → review-rules "Swift / Desktop conventions" (CLI-flag rule); (b) promoted 2026-08-04 → review-rules §8; (c) still open]
- outcome: TBD

## 2026-08-04 — feature/slack-multi-account (PR #58, convergence rounds 2-3 + final judge synthesis) — verdict: approve (code) — SEE CRITICAL note in summary: fixes are stranded, not yet on `main`

- contested: whether round-1's blocker/major calls would survive two more adversarial rounds AND whether the FIX batches themselves introduced anything new — they were real and repeatedly re-litigated. Round 2's fresh prosecutor found 5 NEW issues in fix batch 1 itself (a genuine `-race` data race in the new `--app-return` test polling a plain `bytes.Buffer` across goroutines; `recordAuthResult` never wrote `status="revoked"` so Desktop's `SlackAccount.isRevoked` red-indicator stayed permanently dead for Slack; the new shutdown-guard shipped with zero test despite explicitly claiming to mirror `calendar.Syncer`'s TESTED precedent; a dormant migration shape-gap; a DRY nit) — fix batch 2 closed all 5. Round 3's prosecutor, attacking specifically for false-positive risk in the new revoked-classification and weakened-to-pass test assertions, found ZERO blockers/majors and only 2 minors (revoked-classification untested at the daemon fan-out level; an overclaiming doc comment) — fix batch 3 closed both. [6 AC / 7 tests]
- contested: my own independent re-verification (not just trusting the round-3 summary) of the two highest-risk, least-reviewed pieces: `isRevokedAuthError` (`internal/sync/orchestrator.go`) — confirmed it correctly does structured `slack.SlackErrorResponse` matching first, string-fallback second, over a small explicit code set (`invalid_auth`/`account_inactive`/`token_revoked`/`not_authed`), with the classification only reachable from `Run`'s own top-level return (never from a per-item/per-channel swallow) — no false-positive path found by tracing every Slack-error swallow site. And `TestPhaseSlackSyncRevokedTokenClassification` (`internal/daemon/daemon_test.go`) — confirmed it genuinely exercises the daemon-level fan-out (seeds an account with NO team_id so `team.info` is actually called, unlike the pre-seeded sibling helper) and passes clean under `-race`. Both matched the round-3 summary exactly. [7 tests / 9 error-handling]
- false-positive: none found in my own pass — every round-1/2/3 fix re-verified as genuinely present in code, not just claimed (messages_fts migration rewrite + dedicated `TestMigration00048_PreservesSearchOverExistingMessages`; sender: shape-exclusion + `TestMigration00048_PreservesGmailJiraSenderLearnedRules`; `--app-return` registered on both `slack add`/`slack login`; `filterOwnAccountIDs`/`ownsID` shared helper on both `syncChannelReadState` and `syncInboxReactions`; `resolveSlackLinkTarget`'s dedup+cache in the new `internal/ai/slack_link.go`; dead `currentUserID` Swift field removed cleanly with zero dangling references).
- miss (process, the headline of this run): none in the CODE — but a major process gap surfaced only by checking `gh pr view 58` and comparing trees, not by reading the diff. **PR #58 was already squash-merged into `main` on 2026-08-02T13:20:33Z (mergeCommit `3e56935f`), and its tree is BYTE-IDENTICAL to `b50e3022` — the pre-fix-batch-1 state carrying all 2 blockers + 3 majors round 1 found.** The debate-review loop that found and fixed them ran AFTERWARD (fix batch 1 dated 2026-08-03 16:59, a full day after the PR had already merged) entirely on the local feature branch, which was never pushed past its pre-merge state (`origin/feature/slack-multi-account` == `b50e3022` == the merged tree); none of the 4 fix commits reached any remote or PR. `main` has since taken one unrelated commit (`14868968`, meeting-transcriber work, zero file overlap) and at least one other branch (`PR #60`, mobile-app merge) has already inherited the broken pre-fix state. I verified via a disposable `git worktree` that all 4 fix commits cherry-pick onto current `main` with ZERO conflicts and `go build ./...` stays green — this is a clean, low-risk fast-follow, not a re-review. [8 regression — but really a deployment/process-tracking gap, not a dimension the debate ledger covers]
- weak-dimension: none in the review technique — build/vet/gofmt/full test suite/targeted `-race` suites/golangci-lint all independently re-run clean by me, not just re-quoted from the ledger. The weak point this run was OUTSIDE code review entirely: nothing in this repo's `debate-review` skill checks "is the branch under review still what's actually deployed" before running a multi-round loop — the loop correctly converged on a branch that had already been merged elsewhere under it, and would have shipped stale fixes with a rubber-stamped "approve" had the judge not diffed trees against `gh pr view`.
- rule-gap (candidate, promote — the important one this run): before starting or continuing a `debate-review` convergence loop, check `gh pr view <N> --json state,mergedAt` (or diff the branch tip against `origin/<branch>` and `origin/main`) — if the PR is already merged, the loop is reviewing a POST-MERGE fix candidate, not a pre-merge gate, and the final report must say so explicitly (push + new PR) instead of a plain "approve" that implies the original PR merge is what's being blessed. This is a process check with zero cost (~10s) that would have caught the gap on round 1 already had it been standard practice. [promoted 2026-08-04 → review-rules "Review process"]
- outcome: TBD

## 2026-08-04 — feature/calendar-now-line (round 2, post-fix dd858623) — verdict: approve

- contested: F1 severity (viewport-height resize staleness, CalendarEventsView.swift:158-162) — toolkit code-reviewer filed "important" (conf 82), prosecutor minor, advocate conceded-nit. Settled MINOR, approve-with-follow-up. Judge-verified real: `viewport.size.height` is read only inside the `onPreferenceChange` closure, and a pure-vertical resize keeps the marker's frame in the named scroll space byte-identical (scroll offset anchors to top), so `NowLine.visibility` never re-runs — the everyMinute tick and 30 s poll re-render the SAME frame, no self-heal until the next scroll. Kept minor, not major: narrow trigger (height-only resize with the marker near a viewport edge), benign failure both ways (wrongly-shown button still scrolls correctly; wrongly-hidden button reappears on the first scroll — the exact gesture the affordance exists to prompt), and no data/AC damage. 3-lane agreement pinned REALNESS, but severity was still judged on failure-mode consequence, not lane count. [6 AC / 4]
- false-positive: none dismissed this round — codex returned zero findings, and every filed item verified real (including code-simplifier's dead `markerCount` assertion after exact-array equality in NowLineTests — confirmed unable to fail, kept as nit).
- miss (prosecutor-unique, judge-verified by walking the code): F2 — NowLine.swift:34-36's doc claim "a malformed event … stays above the line" is ordering-dependent and false in general: a timezone-less `start_time` sorts lexicographically at its NOMINAL slot (string ORDER BY) but parses to `distantPast`, so a malformed row after the first future event renders BELOW the line; the test seeds the malformed row first, hardcoding the favorable ordering. Fix = narrow the comment, not the code. Lesson shape: a doc claim about degenerate input must hold under adversarial ORDERING of that input, and its test must not seed only the ordering that makes the claim true. [3 / 7]
- weak-dimension: none this round — dimension 7 was the branch's strength (test-analyzer 9/10, no critical/important gaps; behavior-through-public-API, boundary rationale documented). The round-1 bimodal pattern (pure helper tested, render half untested) was genuinely closed by the fix commit, confirmed by both debaters independently.
- rule-gap (candidate): a SwiftUI classification derived from TWO geometry inputs (a preference-published frame AND a GeometryReader dimension) must recompute when EITHER changes; sampling input B only inside input A's change callback is the staleness tell — `onPreferenceChange` fires on frame change only, so pair it with an `.onChange(of:)` companion on the other input feeding the same update function. dim 6/4. (Companion to round-1's candidate (b) on storing derived discrete state — the derivation was fixed; the trigger set was the residual.)
- outcome: TBD

## 2026-08-04 — feature/calendar-now-line (round 3, post-fix a1234326, convergence close) — verdict: approve

- contested: prosecutor F2 (rule §6 letter — no test fails on pre-fix code for the resize-trigger fix) vs the advocate's infeasibility defense — settled ACCEPTED DEVIATION, judge-recorded: the untestable residue is two one-line trigger calls (`onPreferenceChange` + `onChange(of: viewport.size.height)`) into one fully-tested pure function, ViewInspector-class tooling is not in the house kit, and the dedicated coverage lane (pr-test-analyzer 9/10) explicitly ruled no test required while every newly-testable derivation (`jumpArrowSymbol`) did get its test. Precedent shape: rule §6's intent is satisfied when a fix's LOGIC is extracted-and-pinned and only un-instantiable view wiring remains — but the infeasibility must be argued and recorded (this block is that record), not silently waived. [7 tests / 6 AC]
- contested: prosecutor F1 = advocate concession (b) — `onPreferenceChange` closure is nonisolated/@Sendable in the macOS 15 SDK and mutates the non-Sendable `NowLineFrameBox` + @State; clean under the project's pinned Swift 5.10 language mode, an error under Swift 6 strict concurrency. Settled MINOR, non-gating, batch with the Swift 6 migration — not new exposure (the closure wrote @State pre-fix too), runtime is main-thread today. Third strict-concurrency-shaped item on this small branch (after round-1's `static var defaultValue` candidate): SwiftUI geometry-callback code written under 5.10 minimal checking reliably accrues Swift-6 debt; worth one consolidated migration pass, not per-PR nits. [3 code-style / 8 regression]
- false-positive: none — codex zero findings, toolkit code-reviewer zero at conf ≥80; the two items it deliberately withheld (box lifetime across mode switch conf 55, mixed height sourcing conf 40) were correctly withheld: no observable failure. Simplifier S1 (publish Visibility? through the PreferenceKey, delete the box/onChange dance) judged a valid alternative shape but DEFERRED — the shipped code already meets round-1 candidate (b) (derived discrete state, write-on-change, raw CGRect out of invalidating @State), and a correctness-neutral restructure adjacent to a settled item doesn't gate a convergence round. [4 / 1]
- miss: residual of round-2's own fix — the narrowed doc claim ("may render below the line if it sorts after a future event") shipped without its covering test, and NowLineTests.swift:108's inner comment still carries the retracted unqualified "stays above the line" phrasing; caught by advocate self-concession + simplifier (S5), missed by prosecutor and codex. Small but recurring shape: when a review narrows a DOC claim, the test whose seeding made the old claim look true keeps its stale comment unless explicitly re-read — a doc-narrowing fix should grep the tests for the retracted phrase. [7 tests / 3]
- weak-dimension: none — three-round convergence closed with all lanes agreeing; dimension 7 stayed the branch's strength (9/10 both post-fix rounds).
- rule-gap (proved useful): round-2's two-geometry-inputs candidate was immediately load-bearing — the fix implements exactly its prescribed shape (both inputs funneling one update function) and round 3 verified it in code. Candidate for promotion together with round-1's (b): as a single SwiftUI-geometry rule (derived discrete state, write-on-change, recompute on EVERY input's change path). dim 4/6.
- convergence (FINAL): 3 rounds — R1 changes-needed (fixed dd858623), R2 approve + accepted minor (fixed a1234326), R3 approve with zero gating findings. Judge re-verified a1234326 in code (box + dual-trigger + on-change-only write + jumpArrowSymbol test + narrowed doc), gate green every round (build/lint/full suite exit 0, 13/13 NowLineTests). Remaining: 4 cosmetic nits (stale test comment S5, duplicated box comment S2, doc-altitude S4, test placement S6) + 2 deferred follow-ups (S1 restructure; Swift 6 concurrency batch) — none gating. Ready to merge.
- outcome: TBD

## 2026-08-05 — fix/single-instance-guard (16b04ddb/af617a1c, round 4) — verdict: changes-needed

- contested (the headline, and a self-inflicted arc): round 1 filed payload forwarding as a **Needs-human owner scope call**; the owner took it, and commit 16b04ddb implements it over `DistributedNotificationCenter` — a session-global bus with **no sender authentication** — wired straight into `NotificationDelegate.route`. The remedy for a review finding thereby introduced the arc's only security finding. Judge-verified capability delta (the point the advocate's "the adversary already reads DBs and tokens" defence misses): files under Application Support are not TCC-gated against a same-user process, but the **microphone is** — a forged `meeting_reminder` + `joinRecordActionID` + `eventId` reaches `JoinMeetingAction.join(forceRecord: true)` and starts mic + system-audio capture with **no prompt, attributed to Watchtower's grants**. Settled: merge-blocking regardless of the major/blocker label both debaters converged on. [9 security / 6 AC]
- contested (judge-closed an avenue before the next round re-opens it): "authenticate the forwarded response instead of degrading it" is **not** available at this cost tier — DNC exposes no sender identity; a shared-secret file at 0600 is readable by the same-user attacker by construction; matching against a delivered/pending notification identifier fails too, since the attacker can read the SQLite DB and reconstruct identifiers. Navigation-only degradation or a real authenticated transport (XPC + peer audit-token/code-signing check) are the only two honest options, and picking between them is an owner scope call, not a fixer edit. Recorded so a future round does not spend itself re-deriving it.
- contested (implementation trap the ledger's remedy wording hides): "navigation-only" cannot be implemented by trimming `eventId`/`conferenceUrl` out of `routedKeys`. `actionID` travels in the **envelope**, not the payload, so the `meeting_stop_recording` + `stopRecordingActionID` kill-switch arm survives a payload-only trim untouched. The gate must sit at the forwarded routing entry. Generalizable: when an allowlist and an out-of-band field both feed one dispatcher, an allowlist-shaped remedy closes only half the surface.
- false-positive: F5 (nil-appState silent no-op) and F10 (allowlist drift) correctly dismissed as pre-existing/fail-safe-by-design; F2 self-closes with F1's remedy; F6's transport-loss half rebutted (distnoted owns the message once `post` returns) leaving only an overstated comment. [9/3]
- miss (lane-availability, third consecutive round): codex UNAVAILABLE — codex-cli 0.23.0 rejected by the API (toolchain, not a review failure). Combined with R1–R3's "codex contributed only an echo", this branch has now run its entire four-round arc without a single codex-unique finding. On small Swift/shell diffs the independent-synthesis value is coming from the toolkit specialists (silent-failure-hunter and pr-test-analyzer each carried unique catches again this round), not codex.
- weak-dimension: **7, fourth consecutive round on this branch, and the same bimodal shape every time** — R1 pinned a one-line pure helper while the plist/wiring/script went unasserted; R4 ships a 6-test codec suite (`NotificationForwardingTests`) beside a `route` dispatch table with **zero** tests, where reverting the `meeting_reminder` case to `break` keeps the whole 1619-test suite green. The already-promoted rule ("every new `@Observable` VM/center ships its own suite") does not bite here because `route` is a static func on an old class, not a new VM. The rule needs widening: a **new cross-process/cross-boundary entry point** gets its own suite even when it lands on an existing type.
- rule-gap (candidates, both judge-authored this round): (a) **any cross-process message channel (distributed notification, URL scheme, named pipe, unauthenticated Mach port) is attacker-controlled input, and the actions it can reach must be bounded by what a same-user attacker is already entitled to — TCC-gated capabilities (mic, camera, screen, Contacts/Calendar) are the delta that makes an otherwise "they already own the box" argument wrong**, dim 9; (b) when a review's own remedy adds an IPC/persistence channel, that remedy re-enters review as new attack surface — a finding closed by "the owner will scope it" must be re-threat-modelled when the scope call comes back as code, dim 9/8.
- calibration (advocate lane, **third consecutive round**): F9's "these two build fixes are a compile prerequisite" is factually wrong — judge-verified `Package.swift` is `// swift-tools-version: 5.10` (Swift 5 mode, no strict concurrency) and `GPU.clearCache()` still exists in the resolved MLX checkout annotated `@available(*, deprecated, renamed: "Memory.clearCache")` (`GPU+Metal.swift:169-172`), so both changes are warning fixes. Precedents: R1 "shell scripts untested by repo convention" (wrong), R2 "the space-path fix has portability costs" (wrong). Three rounds, three advocate defences resting on a **greppable repo fact** that dissolved on inspection. This is now a lane-level pattern, not bad luck: the advocate prompt should require a defence citing repo state to quote the command and its output, and the prosecutor should treat an uncited advocate fact-claim as unmet burden rather than a settled point.
- outcome: TBD

## 2026-08-05 — fix/single-instance-guard (4232b2a9, round 5 of the arc / round 2 of the convergence loop) — verdict: approve (post-merge: push + open a new PR, the hole is live on `main`)

- process (the headline, and the second occurrence in two days): **PR #72 was merged into `origin/main` by an external actor mid-review**, carrying the ungated forwarding (16b04ddb) and NOT the security fix (4232b2a9, still local-only). The 2026-08-04 promoted rule ("check `gh pr view <N> --json state,mergedAt` before starting or continuing a convergence loop") fired and the orchestrator caught it — the rule has now paid for itself twice on two different branches. Strengthening candidate: re-check at the START OF EVERY ROUND, not just at loop start; on this arc the merge landed between rounds, so a loop-start-only check would have missed it. Corollary the verdict has to carry: with the defect live on `main`, "approve" inverts its usual cost asymmetry — every additional polish round is time the mic-arming hole stays shipped, so only findings that change behaviour may hold the push.
- contested (judge-settled, and it moves a boundary the debate drew too generously): the fix's own doc calls the gate "navigation only … may move the UI but must never arm an action". Judge-traced: `forwarded` navigation sets `pendingDigestID` → `DigestListView.onChange(selectedDigestID)` → `viewModel.markDigestRead(id)` → a **SQLite write** (`DigestListView.swift:51-55`, `AppState.swift:216-219`). No capability delta (a same-user attacker writes the DB directly; nothing TCC-gated), so not a finding — but the comment over-claims, which is exactly what round 4 punished. Generalizable and worth promoting: **a security gate described as "navigation only" must be evaluated at the UI-reaction layer, not at the routing function — the navigation target's `onChange`/`onAppear` handlers are part of the reachable surface.** SwiftUI makes "just set a @State destination" a side-effecting operation.
- contested (judge-settled against two lanes): the comment at `WatchtowerApp.swift:71-72` — "the `userInfo` keys read below must stay in sync with `NotificationForwarding.routedKeys`" — is itself the regression vector, because the keys read below include the UNGATED branch's `eventId`/`conferenceUrl`; literal compliance re-adds precisely the payload round 4 removed. Raised by two lanes independently (prosecutor F4, code-reviewer F1), which is why it outranks its cosmetic appearance. Rule-gap candidate: **a doc comment that names an allowlist as "what the code below reads" must scope itself to the GATED branch when the ungated branch reads more.**
- false-positive: (a) grace window 2 s→5 s "widens the interception window for legitimate action clicks 2.5×" — the widening is the round-4 remedy for response-arrival latency, and the alternative (duplicate exits first) routes the click through a fresh duplicate on the same degraded path, so there is no un-degraded arm to preserve; (b) `observe`'s non-String `object` guard dropping silently while `decode` logs — consistency-only, and on an unauthenticated bus the existing `decode` log is the floodable one, so "fix" would run the wrong way. [9/8]
- miss (lane-availability, **fourth consecutive round**): codex UNAVAILABLE — codex-cli 0.23.0 rejected by the API. This branch completed a five-round arc with zero codex-unique findings; the independent-synthesis value came entirely from the toolkit specialists (this round: `code-simplifier` alone produced the only strictly-stronger test rewrite, `pr-test-analyzer` the only owner-decision-worthy gap).
- weak-dimension: **7, fifth consecutive round on this branch — but the shape finally changed and should not be re-filed as the same thing.** R4's complaint was an untested *core* (`route`'s dispatch table, zero tests); 4232b2a9 ships `NotificationRouteTests` (9 tests, mutation-verified double-fail on gate removal, anti-tautology negative control). What is left uncovered is the *edge*: `WatchtowerApp.init`'s `forwardMode: isDuplicate` wiring and the `didReceive` forward branch, neither reachable without live UN plumbing (`UNNotificationResponse` has no public init) — genuinely untestable at this cost tier, not neglect. A future round should file the seam extraction (observe-closure body → a testable static) once and then stop.
- calibration (advocate lane, **the round-4 correction worked — record it so it is not reverted**): after three rounds of defences resting on greppable repo facts that dissolved, this round's advocate command-cited every repo-state claim, and its volunteered concessions were judge-verified TRUE rather than defensive (C1 above is a real, correctly-scoped admission the prosecutor did not find). The prompt change ("quote the command and its output") is the cheapest lane fix this log has produced.
- rule-gap: round-4's candidate (a) — cross-process channels bounded by the TCC-gated-capability delta — **drove this fix and held under adversarial re-review with no bypass found; recommend promoting it to `review-rules.md` §9 verbatim.** Round-4 candidate (b) (a review's own remedy re-enters review as new attack surface) also held: the remedy for the remedy is what this round graded.
- outcome: TBD

## 2026-08-07 — feature/tray-daemon-lifecycle (pre-PR debate review) — verdict: changes-needed

- contested (the blocker, and the judge had to settle what the debate left "undecidable"): the prosecutor filed F1 as a race whose winner is unknown from source, and the advocate conceded it on that framing. It is not undecidable — it is decidable and BOTH arms fail. Arm A: `AppState.initialize()` is reachable only from `rootContent.onAppear` (`WatchtowerApp.swift:295`) and the onboarding-completion closure (`Navigation.swift:11`), so a login launch that closes the main window in `applicationDidFinishLaunching` never runs migrations, never opens the DB, never starts the daemon, and never sets `NotificationDelegate.sharedAppState` (which the tray's own quit path reads for `isCapturing`) — the branch's headline feature is inert. Arm B: the main-window predicate keys on `frameAutosaveName == "WatchtowerMainWindow"`, and that name is set from `OpaqueBackgroundView.viewDidMoveToWindow()` (`WatchtowerApp.swift:378`) — a SwiftUI mount side effect that has not run at `applicationDidFinishLaunching` time, so the close loop matches nothing and the login launch shows a visible window with `.accessory` policy (no Dock, no menu bar). Generalizable: **an AppKit-lifecycle predicate that keys on state written by a SwiftUI view's `viewDidMoveToWindow`/`onAppear` is evaluated before that state exists — window identity used at delegate-callback time must come from a launch-time source (`WindowGroup(id:)`-derived `NSWindow.identifier`), not from a cosmetic side effect of mounting.** [6 AC / 1 architecture]
- contested (severity ordering the ledger got backwards): `code-simplifier #1` entered as a DRY observation ("AppState's daemon-stop closure duplicates stopForQuit") and only the advocate's rebuttal round elevated it to what it actually is — `DaemonManager.stopDaemon()` routes through `runProcess`'s unbounded `waitUntilExit()`, and it sits on the launch path before the DB opens, so a hung `sync stop` parks the app on the splash forever. The same file ships the correct bounded shape (`stopForQuit`) 40 lines away. Shape worth remembering: **a duplication finding whose two copies differ in their timeout/cancellation posture is a liveness finding, not a DRY finding — grade the divergence, not the repetition.** [4 efficiency / 9 error-handling]
- false-positive: F11 (onboarding-pending launch stops the daemon and never restarts it) — dropped correctly and judge-re-verified independently: `OnboardingView.swift:954` calls `startPipelines`, whose phase 3 unconditionally launches `sync --daemon --detach` (`BackgroundTaskManager.swift:227-230`), so the daemon does come back; residual is only the AI-pipeline-length gap before it does. Also correctly withdrawn: F12's unreaped-child and "prevented logout" arms (the prosecutor reproduced `Process` self-reaping and confirmed 12 s is the CORRECT bound — the old 2 s killed the CLI mid-SIGTERM-grace per `cmd/sync.go:129-134`); an author whose widened timeout looks like a regression was right and the review nearly punished the fix. [6/9]
- miss: the prosecutor's F7 ("the DaemonSettings Error section is dead code") was filed as a docs nit, but the real shape is a **capability regression**: removing the Start/Stop toggle removed the only writer of `DaemonManager.errorMessage` that view could ever observe, while `ensureDaemonRunning()` writes its failure into a throwaway `DaemonManager()` instance nobody reads — so a failed daemon start is now invisible on every surface AND unretryable without an app relaunch. Caught as three separate small items across three lanes (F6, F7, code-reviewer M3/M5) and only merged into one real finding at synthesis. [9 error-handling / 6]
- weak-dimension: **7, and the same bimodal shape this log has now flagged for six consecutive arcs on Desktop work** — `CLIBinaryStore` (a pure, injectable helper) got a 6-test suite while `TrayAppDelegate`, the file carrying every destructive and lifecycle-critical decision on the branch, got zero: deleting the `managesLifecycle` guard from `applicationShouldTerminate` (which is what stops a SingleInstanceGuard duplicate from killing the survivor's daemon) leaves the whole 126-test Swift suite green. The promoted rule ("every new `@Observable` VM/center ships its own suite") again does not bite, because an `NSApplicationDelegate` is neither. Second consecutive arc where the rule missed for a type-shape reason; it should be widened to "**every new lifecycle/cross-boundary entry point**", not re-filed a third time.
- rule-gap (candidates): (a) **a signed app that relocates its own executable payload out of the bundle to a user-writable path must validate that payload at EXEC time, not only at install/sync time** — a hash checked during `sync` and an `isExecutableFile` check at `installedPath()` are not the same guarantee, and the gap is the whole reason the copy exists, dim 9; (b) **a doc comment or user-visible error that names a fallback ("falling back to the bundle CLI") must be traced through the resolver it claims to describe** — here `findCLIPath()` prefers the store copy unconditionally, so the stale copy wins and both the comment and the tray's error string are false at exactly the moment they are shown, dim 3/9; (c) a change that moves a spawned binary out of the `.app` bundle touches the TCC responsibility chain and must carry an explicit manual TCC observation item — this branch's spec and plan contain zero occurrences of "TCC", on a repo whose standing policy makes a Watchtower.app TCC prompt a P0, dim 9.
- outcome: TBD

## 2026-08-08 — fix/voice-match-attendee-scope (pre-PR debate review, fast mode: no rebuttal round) — verdict: changes-needed

- contested (the blocker, and the judge had to widen what three lanes filed as one shape): owner identity is `ownerEmails.contains(match.personKey.lowercased())` (`MeetingRecorderCenter.swift:488`), but `person_key` is an email ONLY when `SpeakerNaming.personKey` found an attendee match (`VoicePrint.swift:88-97`); every other rename — ad-hoc recording (`RecordingDetailView` passes `attendees: []`), free-text name, attendee row without an email — mints a NAME-keyed print. Judge-verified the reachability the lanes asserted but did not prove: `MeetingTranscriptQueries.renameSpeaker` validates only the TARGET label against `isReserved`, so renaming FROM «Я» is allowed and is exactly how an owner print gets minted. A name-keyed owner print therefore lands in `voiceNames[best]` without landing in `ownerClusters` → the veto fires against the owner → «Я» disappears from every future recording once any Google account is connected. The generalizable shape: **an identity check that keys on a field whose format is CONDITIONAL (email here, name there) silently inverts its own safety mechanism on the branch where the format differs** — the veto is armed by `ownerEmails` being non-empty (identity "known") but can only ever be satisfied by the email-shaped subset. [6 AC / 2 architecture]
- contested (judge-resolved against the prosecutor, no change required): F2 — "the veto returns nil with no fallback to the next mic-dominant candidate carrying no foreign voice name". Falling through would hand «Я» to an unidentified runner-up on a guess, which is precisely the wrong-«Я» outcome the owner-approved design ranks as worse than a missing one. The conservative nil is correct; only its silence is a finding (no diagnostic when the veto fires, unlike the mega-cluster suppression 25 lines away, which prints).
- contested (judge-verified the refactor-equivalence claim both sides asserted): old `detectSelfCluster` took the global max share then applied `> selfShareThreshold`; new code filters by threshold first then maxes over `order`. Equivalent for all inputs — same element, same earliest-wins tie-break, same nil when nothing clears. The advocate was right and the prosecutor's F7 correctly reduced to a coverage gap, not a behavior risk.
- false-positive: none filed that the judge overturned outright; the closest is F2 above (a real observation whose remedy runs the wrong way).
- miss: **codex produced the round's only unique architectural catch** (organizer is a separate `organizer_email` column, never inside the `attendees` JSON — judge-confirmed at `internal/calendar/client.go:307-318`), so an owner-as-organizer event yields `ownerClusters == []` (armed, unsatisfiable) rather than nil. First codex-unique finding in this log since the toolchain upgrade; worth noting against the four preceding "codex UNAVAILABLE / echo-only" entries. Second near-miss: only the toolkit reviewer noticed `docs/app-guide.md:111` ("«Я» detection always wins over a voice match") is now false — a doc injected into the chatbot's system prompt, covered by CLAUDE.md's App Guide Maintenance rule, which no lane's checklist encodes.
- weak-dimension: **7, seventh consecutive Desktop arc with the identical bimodal shape** — 6 new `RoleAssigner` tests + 4 new `VoicePrintMatcher.scoped` tests (the pure layer, thorough) beside ZERO `MeetingRecorderCenter` tests, although `MeetingRecorderCenterTests` already owns a `runDiarizationFlow` harness. The untested layer is where the blocker lives: `ownerKnown ? ownerClusters : nil`, the `personKey` compare, the `mega ∩ ownerClusters` filter and the `eventID` plumb-through are all unasserted, and inverting `ownerKnown` keeps the suite green. The promoted rule ("every new `@Observable` VM/center ships its own suite") missed for the third arc running, this time because the center is not new — **it needs to read "new decision logic on an existing center/entry point", not "new center"**.
- rule-gap (candidates): (a) **when a change makes a previously-absolute rule conditional, the identity/gate value it now keys on must be reachable from EVERY writer of that value** — grep the mint sites, not just the read site (the name-keyed-vs-email-keyed split above); (b) **a filter applied to a pool before a classifier reads that pool must be checked for filtering out the classifier's own reference subject** — attendee scoping runs before owner classification, so an owner who is not a guest on their own event is scoped out of the pool that decides whether they are the owner; (c) a user-visible change to a behavior documented in `docs/app-guide.md` updates the guide in the SAME change (the guide is injected into the chatbot system prompt, so a stale line becomes a wrong answer, not just wrong docs) — CLAUDE.md states this but no lane checklist encodes it.
- outcome: TBD

## 2026-08-08 — fix/voice-match-attendee-scope (round 2, post-fix da0a7c8c, fast mode) — verdict: changes-needed

- contested (the blocker, and it is the round-1 FIX that regressed): `attendeesIncludingOrganizer` (`CalendarEvent.swift:125`) appends the organizer to an EMPTY attendee list, so a self-created zero-guest event — the extremely common "block the calendar for a call with someone who is not a Google guest" pattern — now yields a one-element identity set, `scoped()`'s `guard !attendees.isEmpty` no longer fires, and the pool collapses to the owner's own prints: every colleague silently becomes `Speaker N`. Pre-change the same event produced `[]` → global pool. `parsedAttendees` folds UNDECODABLE attendee JSON into the same empty return (`(try? decode) ?? []`), so a corrupt blob plus an organizer lands in the identical organizer-only pool — the **sixth** recurrence of the absent-vs-undecodable class already promoted to `review-rules.md` §Error handling, arriving this time not as a missed third state but as a fix that made the folded state actively harmful. Generalizable: **widening an identity set that a downstream emptiness guard uses as its "unknown → degrade globally" sentinel destroys the sentinel; check every `isEmpty` guard reading a collection you just made non-empty.** [6 AC / 9 error-handling]
- contested (three lanes, judge-confirmed by grep): the round-1 organizer union landed only on the READ side — the voice-print MINT site (`RecordingDetailView.swift:275`) still calls `parsedAttendees`, so an organizer-not-guest is never offered in the rename picker, their print stays NAME-keyed, and `scoped()`'s email-keyed compare cannot see it. The fix therefore **delivers none of its benefit in its motivating case while shipping the harm above**. Shape worth remembering: when a fix adds a new accessor for an identity set, grep every consumer of the old accessor in the same change — a read-side-only union is not a union, and its accessor-level unit test (`testAttendeesIncludingOrganizerAddsNonGuestOrganizer`) passes while the end-to-end scenario is untouched, which is exactly the §6 "test that fails pre-fix" letter being met at the wrong altitude.
- contested (judge-settled AGAINST the prosecutor, no change required): F5 — `filterMegaClusters` leaving `owners` non-nil-empty after suppressing the blob, rather than collapsing to nil. Collapsing would disarm the veto, and the only reachable shape (blob suppressed, some OTHER cluster mic-dominant and confidently named) is one where the mic-dominant winner really is a named colleague — restoring «Я» there is precisely the wrong-«Я» the design ranks worst. `testMegaClusterLosesOwnerStatusWithItsName` correctly pins intent; per the 2026-08-04 process rule this is a test encoding INTENT, not a residual defect, and a future round should not re-open it.
- false-positive (calibration, simplifier lane): "`AppState`'s `.lowercased()` is provably dead" — true as a data-flow claim (`isOwnerPrint` normalizes symmetrically and a test pins it) but wrong as a recommendation: it is boundary normalization on a `Set<String>` that carries no type-level contract, and the round-1 blocker was itself a normalization/format mismatch across exactly that boundary. Declined. Same lane's `loudest → max(by:)` also declined (the loop's strict-`>` earliest-wins tie-break is the subtle part; a one-liner hides it).
- miss (round-1's own fix, residual): `ownerArmed` is pool-level (`MeetingRecorderCenter.swift:487`) while `ownerClusters` membership keys on `bestMatch`'s single GLOBAL winner, so an owner holding BOTH a name-keyed and an email-keyed print can have the name-keyed one win, land in `voiceNames` but not `ownerClusters`, and be vetoed out of their own «Я» — the very failure the fix's doc comment claims it prevents. Caught by the toolkit reviewer and volunteered by the advocate; not by the prosecutor. This is the 2026-08-04 promoted process rule ("verify every gate variable the new invariant READS") firing on a comment written *by the previous round's fix* — a fix's own safety-argument comment deserves the same adversarial read as the code.
- weak-dimension: **7, eighth consecutive Desktop arc, and the bimodal split moved rather than closed.** Round 1's complaint (thorough pure layer, zero `MeetingRecorderCenter` tests) was genuinely fixed — three real Center-level tests now drive the whole diarization flow. What is left untested is one layer further out and is where both blockers live: `AppState.wireMeetingRecorderLoaders` has zero tests and swapping `attendeesIncludingOrganizer` back to `parsedAttendees` keeps all 1758 tests green. The 2026-08-04 "un-instantiable view wiring" infeasibility precedent does **not** cover it — `AppStateTests` already builds an `AppState` with a test `DatabaseManager` and calls `initDashboard` (made non-`private` for exactly this reason), so the same one-word change on `wireMeetingRecorderLoaders` makes it testable at zero architectural cost. The lane pattern across eight arcs is now precise enough to state as a rule: **the untested layer is always the outermost one the fix touched**, because each round's coverage fix stops at the layer the previous round named.
- rule-gap (candidates): (a) the emptiness-sentinel rule in the first bullet — dim 6/9, and it is the concrete generalization of round-1's candidate (b) (a filter running before a classifier), so the two should be promoted together or not at all; (b) **a diagnostic added because a silent decision "looked like the feature broke" must be emitted where the decision is ACTED ON, not where it is computed** — the round-1 veto `print` landed inside `RoleAssigner` (header: "No I/O", CLAUDE.md: pure) and fires twice per recording because `clusterLabels` is called by both `assign` and the save path; the mega-cluster precedent it cites prints from the Center, once, dim 1/3.
- outcome: TBD
