# Memory feature — autonomous execution protocol

> Owner directive (2026-07-16): phases proceed autonomously, without owner interaction.
> This file defines how, and hosts the running decision journal the owner reviews at will.

## Operating rules

1. **Phase cycle** runs unattended: spec → implementation plan → agent-wave implementation (TDD) → adversarial review to convergence → fixes → push to `feature/secretary-memory` → CI kept green by a shepherd agent. No step waits for the owner.
2. **Judgment calls** are decided by conservative-default-per-spec and recorded below with rationale — never asked. The owner overrides asynchronously by editing/commenting; overrides are then implemented as ordinary work items.
3. **Inventory contracts** are never weakened silently. When a phase must CHANGE a contract, the change lands with an extended (never weakened) guard test and a `NEEDS-OWNER-REVIEW` entry below. Retroactive veto beats stalled work.
4. **Work-machine validation is deferred to ONE full-feature run at the end** (owner directive 2026-07-16): phases 3 and 4 are implemented back-to-back without waiting for live validation between them. Risky semantic output ships dark behind flags (`memory.semantic.enabled`, Phase 4's own flag) so the integration branch stays safe to build on. Every phase contributes its validation section to the living task file `docs/specs/memory-final-validation-task.md`; the owner triggers it once when the whole feature is ready.
5. **Merge to main** happens once, at full-feature completion, by the owner. Everything else merges into this integration branch.
6. Session death (usage limits, machine sleep) pauses autonomy; a single "продолжай" resumes it — all state lives in this branch.

## Decision journal

### 2026-07-15
- Access stats excluded from Phase 3 retention scoring (production counters are write-dead under read-only MCP; eviction must not depend on them until Phase 4 wires a writable path). Conservative default; revisit in Phase 4.
- PR #36 converted to draft: integration-branch mode per owner.

### 2026-07-16
- **NEEDS-OWNER-REVIEW (pre-existing, from work-machine session):** MEM-04 isolation granularity changed from per-channel to per-batch by the quiet-channel batching (`memory.extract_episodes_batch`); inventory updated there as "owner-approved". Recorded here for the audit trail.
- Watermark-persistence-after-kill investigation launched (top E2E blocker); fix lands with a contract-grade test when root cause confirmed.
- E2E-confirmed Phase 3 spec revisions to fold in: map.md two-tier design (56KB observed vs 2KB assumed at 447 entities); episode dedupe keyed on provenance-ref overlap, NOT title similarity (E2E's own duplicates had differing titles); entity-vocabulary broadening (1/447 back-links — hints don't overlap natural-key aliases) becomes an explicit Phase 3 goal rather than an open question.
- Owner directive: no per-phase work-machine runs — phases 3–4 implemented back-to-back; validation consolidated into one final full-feature run (`memory-final-validation-task.md`, living document). Consequence accepted and recorded: Phase 4 is built on live-unvalidated Phase 3 semantic output; mitigations — dark flags, fixture-heavy tests, and the final run validating both layers together before any main merge.
- **Phase 4 review triage (2026-07-16), deferred items (no code change):**
  - **m5 — briefing same-day double-count.** `gatherMemoryRevisions` counts `## History` bullets by date (day-granular, lexical `YYYY-MM-DD`), so two briefings on the same day would both count the same day's entries — the confidence-delta notability could double-count within one day. A precise fix needs timestamped `## History` entries (time-of-day, not just date) to window by the exact previous-briefing instant. Deferred as Phase-5 polish; the day-granular window is acceptable for v1 (briefings are daily).
  - **m6 — dispute item ambient class.** Memory dispute items inherit `DefaultItemClass("decision_made")` = `ambient`, so they auto-seen/auto-archive after 7 days like other ambient signals. Whether a belief dispute should instead be `actionable` (14-day stale window, never auto-seen) is a validation-time tuning question — leave at ambient until the final run shows disputes getting buried; revisit then.
- **NEEDS-OWNER-REVIEW (Phase 3 review, deferred product question):** the ingest floor (`workspace.memory_last_ingested_situation_id`) can pass a *dismissed* situation's id. If dismissal is ever non-terminal — a dismissed situation later transitions to `done`/`converted` — its finalize would be skipped (the floor already advanced past it), so its episodes would never be ingested into memory. This is safe only while dismissal is terminal. Confirm dismissal is terminal, or add dismissed situation ids to the floor-blocking set so a later revival still ingests. (Raised deferred by the Phase 3 triage; no code change made pending owner confirmation.)

### 2026-07-16 (continued)
- **Working decision (owner brainstorm, pipeline convergence):** the two-distillation-stack redundancy (digests/decisions vs memory) is real but non-urgent (~20% AI-budget overhead measured). Option "memory on top of digests" is empirically dead (audit: 99.4% hallucinated links, no outcomes, coverage holes). Target architecture = the inversion: digests/decisions/briefings become RENDERS over memory episodes (validated provenance kills the key_messages hallucination class for free; raw stream read once). Staged as **Phase 5**, post final validation, one consumer at a time (digest-from-episodes dark-launched vs legacy first; people_cards ← person pages; running_summary ← channel ## Current; decisions → episode outcome facet), each step with its own inventory-contract update. NEEDS-OWNER-REVIEW: confirm Phase 5 direction before its spec is written.
- Phase 4 panel converged (round 2 residual = M4 over-scope, fixed with the reviewer's prescribed gate + pinning test; round-3 formality waived for a one-line gated fix applied verbatim). Secondary notes journaled: chronic beliefs_max cap-hit starves the chat-turn floor (safe direction, watch at validation); revived dispute items may retain a stale terminal-situation signal link (benign, DASH-01 dedups).
- **RESOLVED (owner, 2026-07-16): Phase 5 direction confirmed** and expanded to the universal-substrate scope (all external sources + all internal dialogs through memory; internal entities mirrored/linked). Owner constraint: implementation in a separate branch (`feature/memory-phase5` off main post-ship). Spec: docs/superpowers/specs/2026-07-16-memory-phase5-universal-substrate-design.md. The earlier NEEDS-OWNER-REVIEW on Phase 5 is closed.
- Owner addition to Phase 5 (2026-07-16): interaction ingest as sub-phase 5D — owner interactions (feedback, dismissals, conversions, access trail) become memory input via an `act:` resolver scheme and a new code-minted `owner-action` evidence rank; preference beliefs form through the standard belief machinery. Boundary with inbox_learned_rules preserved. Spec updated (5D + MEM-15).
- Owner override (2026-07-16): Phase 5 starts NOW — `feature/memory-phase5` cut from the integration branch (not from post-ship main as the spec's rollout said); it will rebase/merge-forward when PR #36 ships. First slice: resolver registry (MEM-12) + 5A gmail source + 5D mechanical interaction ingest.

### 2026-07-16 (Phase 5 slice-1 review fixes)
- **Applied (TDD):** gmail thread idempotency via a stable `gmailthread:<thread_id>` alias (re-extraction UPDATES in place, union of refs — never a duplicate); sender-seeding noise gate (`SeedConfig.MinMessages` threshold + machine-sender local-part filter); poison-thread cap (newest-N truncation reusing `max_window_messages`, with a prompt note); interaction ingest moved to its own Run step gated ONLY on `sources.actions` (semantic-independent) and gmail-sender seeding gated on `sources.gmail` (independence now literal); `DropMemoryIndex` re-enables FKs loudly (log+return); belief-surface registry trimmed to chat+act with extractor/gmail using scheme-scoped registries; verdict re-scan bounded to a 14-day window and dedup-keyed on situation-id+verdict-TEXT (updated_at moves no longer double-count); engagement net clamped to [-3,+3], per-(feedback,entity) bump dedup, transactional all-or-nothing bumps that HOLD the feedback floor on failure; `last_interaction_at` stamped RFC3339; gmail helpers propagate errors (gmail_messages is migration-guaranteed); plus the style minors (joinAnd→strings.Join, unexported registry types, act drop-logging parity at the resolver, GetEngagement doc as a test seam, inlined gmailExtractSource, cached ChatTablesPresent per validateChatRefs call).
- **Deferred (no code change this slice):**
  - Malformed `internal_date` rows are silently excluded from the gmail extract/seed scans (`internal_date != ''` + `strftime('%s', …)` yields NULL/garbage → filtered). Acceptable because the Gmail sync guarantees RFC3339 `internal_date`; inventory note only, no defensive parse.
  - `ingestInteractions`'s `fold` closure carries several responsibilities (mapping, novelty gate, staging, bump collection); splitting it into named helpers is readability polish, deferred.
  - `gmailTSUnixExpr` (`CAST(strftime('%s', internal_date) AS INTEGER)`) is recomputed per row and defeats an index on `internal_date` (full-scan). Fine at current gmail volumes; a generated/stored unix column is the fix when it matters — comment-only note deferred.
- Slice 1 converged (round 2; 17/17 fixes verified, two P1 attack vectors closed). Orchestrator calibration per protocol: gmailSenderMinMessages=3 (email-specific floor — the reused Slack floor of 20 made sender seeding inert; reviewer-recommended range 2-3). Final validation will judge both noise gates on live mail.
- Slice 2 panel verdict: CONVERGED-as-is (no blockers/majors; all seven priority attacks held). Orchestrator applied the two optional minors inline (pre-build calendar failures now record a step row + counter like build failures; maxEnd-vs-swept rationale documented). Nit-level items accepted as documented tradeoffs.
- **NEEDS-OWNER-REVIEW (slice 3):** MEM-02's reindex-equivalence dump will be EXTENDED to include the new memory_provenance index table (a strengthening — more derived state proven rebuildable, populated in the same UpsertMemoryNode tx as aliases/FTS). Guard test extended, never weakened. Veto by editing the slice-3 plan.
- Slice 3 panel verdict: CONVERGED-as-is (variadic-provenance trap verified clean — all writes funnel through two provenance-passing call sites; window boundaries and coverage ts-matching proven consistent). Orchestrator applied the polish inline: fresh-shadow short-circuit (no repeated haiku spend on the 48h lookback, CompareStats.Skipped), CLI --out parent-dir creation. ListDigestShadow kept as the read-side pair/test oracle. MEM-13 gap-existence scope accepted as documented.

### 2026-07-17 (final validation merged forward)
- Integration branch's final-validation fixes (610ea8b: mechanical entity linking, Known-subjects block for propose-new, DropMemoryIndex clears dispute flags) merged into feature/memory-phase5. DropMemoryIndex semantics unioned: FK toggle (slice 1) + provenance clearing (slice 3) + dispute-flag clearing (validation fix); engagement/hints still survive reindex by design.

### 2026-07-17 (Phase 5 slice-4 judgment calls — 5C mirrors + read surfaces, 5D preference beliefs)
- **Flag names (conservative-default-per-spec):** `memory.sources.operational` (the mirror source), `memory.surfaces.day_plan` + `memory.surfaces.meeting_prep` (the two read surfaces), `memory.semantic.preferences` (the OWNER ACTIONS belief-pass block). All four default false, each an independently-dark byte-identical no-op when off, siblings of the existing `sources`/`surfaces`/`semantic` gates.
- **Mirrors-as-entities under ONE flag.** Targets and tracks share `memory.sources.operational` (one mechanical reader-of-relational-state step, one blast radius) rather than per-table flags — a target/track is a long-lived state machine, so the `entity` kind (What/Current/Facts/Links/Open loops) is its natural mirror, and its *stories* already reach memory as episodes via situations/conversions. No watermark: bounded re-scan (all non-dismissed rows + a 14-day terminal window) + content-equality no-op, because targets/tracks are small mutable tables a floor would mis-track.
- **User-message OWNER ACTIONS block instead of a `revise_beliefs` v2.** 5D preference beliefs ride a gated USER-message block with inline instructions; the system template `memory.revise_beliefs` stays byte-untouched at v1 (the Phase-4 OWNER SAID precedent). This is what makes `memory.semantic.preferences=false` byte-identical — a v2 system template would leak different bytes with the gate off. No new prompt id, no `add-ai-prompt`/light-tier work; the pass stays strong-tier, `beliefs_max`-capped. No new rank/machinery — slice 1 already shipped the `owner-action` rank, the `actResolver`, the staging, and the mint; slice 4 only makes the model SEE the staged `act:` refs it may cite.
- **Two prompt version bumps:** `day_plan.generate` v2→v3 (16th Sprintf slot, `MEMORY OPEN LOOPS`) and `meeting.prep` v3→v4 (11th Sprintf slot, `MEMORY`) — the `briefing.daily` v6 gated-placeholder precedent; a new slot always receives a string (the sentinel when dark), so arg-count integrity is testable and gate-off output is stable. These are the only prompt-store changes in the slice.
- **ACTIVE TASKS dedupe wording.** The day-plan prompt's memory block carries an explicit instruction to deduplicate against the TARGETS section — memory open loops add cross-universe context (tracks, converted stories), they are not new schedulable tasks; `gatherTargets` stays authoritative for schedulable targets (MEM-14: memory is never the write/authority path for operational state), so the model never double-schedules a target it already has.
- **Zero guard-test edits** in the slice (unlike slice 3's MEM-02 extension). The only change to a pre-existing test file is two compile-forced `nil` args threaded into `buildReviseBeliefsPrompt` calls in `beliefs_test.go` (its assertions byte-unchanged). MEM-14 is a brand-new numbered contract with two new `TestMemory14_*` guards (isolated step + full Run); no existing MEM/INBOX/DASH/TRACKS/CATCHUP assertion was touched.
- **NEEDS-OWNER-REVIEW (slice 4):** preference beliefs let the model propose beliefs about the OWNER from ambiguous mechanical interactions (dismissals, conversions) — dark behind `memory.semantic.preferences`, same belief math, same 0.6 birth cap, same non-protecting `owner-action` rank, same journal/dispute visibility, but a **semantics shift** worth the owner's eyes (the memory now forms opinions *about the owner*, not just about the owner's world). Veto by editing the slice-4 plan.

### 2026-07-17 (slice 4 review round 1)
- **Window → alias-set redesign (F1/F3/F13, panel-found; the plan's 14-day-window design was DEFECTIVE).** The mirror candidate window was broken three ways: (a) `DismissTrack` (Go + Desktop) stamps `dismissed_at` but never `updated_at`, so a stale-dismissed track fell out of the 14-day window immediately and its mirror kept `Status: active` + populated `## Open loops` forever; (b) `ListTargetsForMirror`'s `status != 'dismissed' OR updated_at >= ?` scanned `done` targets unconditionally forever, and `conversionLinks` ran before the terminal skip so even skipped-for-good rows paid queries; (c) any pipeline downtime > 14 days stranded already-built mirrors stale. **Fix:** the window is DROPPED entirely — `ListTargetsForMirror()`/`ListTracksForMirror()` take no arg and return ALL rows; the step loads the existing-mirror alias set once (`db.MirrorAliasNodeIDs()`, one SELECT over `memory_aliases` for `target:%`/`track:%`) and per row skips a **terminal row with no mirror** immediately (before `conversionLinks`, before any body work — preserving "terminal before the source was ever enabled is never mirrored"), refreshing every other row (a terminal row WITH a mirror always refreshes; byte-equality keeps it free). The `mirrorRescanWindowDays` const is deleted. The dismissed-long-ago-never-mirrored skip test still passes under the new rule (extended, not weakened); two new tests prove the stale-dismissed-track and long-done-target refreshes.
- **Mirrors excluded from the rewrite tier (F2 — the plan's "front door" claim REVERSED for rewrite; beliefs unaffected).** The plan said a converted mirror "enters the rewrite/belief scope through the front door with no special-casing." That is a churn loop: a converted mirror is `linkedEpisodes`-eligible, so `RewriteEntityPages` would burn a strong-tier call rewriting `## What`/`## Current`, which the next mechanical re-scan reverts (deleting the rewrite's `Provenance:` marker) — permanent churn. **Fix:** `RewriteEntityPages` now EXCLUDES nodes in the mirror alias set (reusing the F1 helper, one batch query). Mirrors stay a BELIEF-scope subject (subject mapping unchanged); only the rewrite selection changed. Inventory (memory.md) and the plan's Architecture bullet's "front door" wording corrected here rather than by rewriting plan history.
- **F14 (gate-off invariant overstated).** Corrected memory.md + validation Section 6: the day-plan/meeting-prep gate-off prompt is byte-identical to the **v3/v4-with-sentinel** render, NOT "byte-identical to slice-3 apart from a version label" — the bumped default templates carry the new sentinel-bearing MEMORY sections themselves. The belief-pass user message IS still byte-identical to slice 3 (revise_beliefs unbumped).
- **Applied minors (round 1):** F6 — a snoozed target's `## Open loops` bullets are tagged `(target snoozed)` so the day-plan surface has status context (snoozed targets are absent from ACTIVE TASKS, so the dedupe instruction cannot fire). F7 — the day-plan `gatherMemoryOpenLoops` `ReadNode` error now logs (guarded; dayplan.New does not normalize a nil logger) instead of a bare `continue`. F11 — the `act:` timestamp is threaded as an int64 from the producer row into `stagedAction.tsUnix`, removing the `strconv.ParseInt` swallow. F12 — the conversion cross-link dedupes by episode **id** (`[[<epID>` prefix), so a later episode-title change no longer accretes a duplicate link line. Plus the style cleanups (F10/S1–S8): plain `[]Node` append in the mirror commit path (mirrors never share nodes), dead `p.logger != nil` guards dropped in `internal/meeting/memory_context.go` (meeting.New normalizes nil), one exported `memory.SectionBullets` replacing the duplicate day-plan/meeting section scanners, `trackSubjects` doc alias fix (`target:`→`track:`), the phantom `mirrorSubItem` doc rename, an unreachable `sb.Len()==0` branch removed, and the dead `len(ownerActions.lines) > 0` half of the belief-block condition dropped.
- **DEFERRED (no code change this round):**
  - **F5 — reflection `## Current` note on a mirror subject is ephemeral.** A mirror regenerates `## Current` wholesale each re-scan, so a weekly-reflection note landed there is wiped by the next mirror run (the interaction-ingest still-open-situation caveat, generalized). Routing the note to `## Facts` (mirror-untouched) instead would touch MEM-11 semantics (which surface writes which section), so it is documented as a known ephemeral-annotation caveat in memory.md rather than fixed. The durable signal is the belief graph + `memory_engagement`.
  - **F8 — per-entity `ReadNode` per day-plan run.** `gatherMemoryOpenLoops` reads each active entity node once per day-plan generation. Bounded (once daily, capped output); optimization (a section-index column, or a bulk read) deferred until it matters.
  - **F9 — stored customized prompts at the same version get `%!(EXTRA` on the two bumped templates.** A user who customized `day_plan.generate`/`meeting.prep` at the old version keeps their stored text, which lacks the new Sprintf slot, so the extra arg renders as Go's `%!(EXTRA string=…)`. Pre-existing store upgrade-model class (the `briefing.daily` v6 precedent) — not a slice-4 regression; journal-only, no code change.

### 2026-07-17 (slice 4 review round 2)
- **Panel round 2 verdict: no blockers, no majors.** The round-1 window→alias-set redesign held under attack; only minors surfaced, all applied inline:
  - **Alias-map threading (SG-1).** `mirrorNode` no longer runs a per-row `LookupMemoryAlias` — `buildOperationalMirrors` already loads the full `MirrorAliasNodeIDs` alias→node_id map once, so the preloaded existing node id + exists flag are threaded through `mirrorSpec`/`targetMirrorNode`/`trackMirrorNode`. Safe because the mirror step is the only writer of these aliases and commits after the loop, so the map stays authoritative for the whole scan (verified by the round-2 prosecutor). One fewer query per row; test assertions untouched.
  - **Doc honesty (R2-F1).** The `## Facts` narration in `mirror_ingest.go` and memory.md ("rewrite-tier-owned / the semantic rewrite tier owns it") was stale since mirrors were excluded from the rewrite tier (round-1 F2). Corrected to: `## Facts` is preserved verbatim; with mirrors excluded from the rewrite tier its only writer is the owner's own vault edits (MEM-03).
  - **Doc honesty (R2-F2).** The implemented skip predicate is broader than the documented "terminal before the source was ever enabled" — it is "a terminal row with NO existing mirror is never mirrored." Header comment + memory.md corrected to the implemented predicate, with the consequence recorded as a **known limitation**: a target/track created AND driven terminal between two pipeline runs never gets a mirror (no existing alias at scan time); its story still reaches memory via the situation episodes — a conservative default, no counter.
  - **GLOB anchoring (R2-F4).** `MirrorAliasNodeIDs` now anchors the id numerically (`GLOB 'target:[0-9]*'` / `GLOB 'track:[0-9]*'`, GLOB case-sensitive, no escaping) so a model-minted concept alias like `target:foo` can never land in the mirror set. Focused test added (`TestMirrorAliasNodeIDsNumericAnchor`: `target:notanumber` excluded, `target:12`/`track:7` returned).
  - **Comment/style nits (R2-F5, SG-2).** `appendCrossLink`'s doc corrected to say it dedupes body-wide (`strings.Contains(body, "[["+epID)`), acceptable because a mirror only emits that token in `## Links`; the snoozed-target check is `t.Status == "snoozed"` (the DB CHECK constrains status to exact lowercase — `EqualFold` kept only where input is untrusted JSON).
- **DEFERRED (R2-F3 — journal only, no code change):** every terminal row that still has a mirror pays a `ConvertedSituationIDs` query + `ReadNode` + byte-compare each run, over a monotonically growing set (terminal rows accrete mirrors forever now that the window is gone). Accepted as the correctness-for-cost trade of the round-1 window redesign (the window's cheapness came at the cost of stranding stale mirrors). Revisit with an index-level terminal marker (a `terminal` column or a settled-mirror alias tier) only if mirror count grows into the ~1k range.

### 2026-07-17 (slice 4 review round 3)
- **Panel round 3: one verified minor (R3-F1) + one nit (R3-F2), both closed by a single query change.** SQLite `GLOB 'target:[0-9]*'` anchors only the FIRST id character (verified live: `'target:1abc' GLOB 'target:[0-9]*' = 1`), so a digit-leading model-minted concept alias could still land in the mirror set — and the round-2 test seeded only the letter-leading counterexample, structurally missing the residual. `MirrorAliasNodeIDs` now uses `LIKE 'target:%'` (keeps the alias-index prefix search — the bare GLOB had also flipped the plan to a full table SCAN, R3-F2) `AND alias != 'target:' AND alias NOT GLOB 'target:*[^0-9]*'` (whole-id digit anchoring), track twin symmetric. Test extended with `target:1abc` and bare `target:` seeds.
- Round-3 ledger otherwise clean: alias-map threading holds (single writer, commit-after-loop; DropMemoryIndex/first-run/deleted-node paths verified), doc corrections accurate, nothing `-race`-invisible. Awaiting round-4 confirmation on this two-line delta.

### 2026-07-17 (slice 4 review round 4 — CONVERGED)
- **Panel round 4 verdict: CONVERGED.** Predicate correctness, query plan (LIKE restores the two index range searches — verified via EXPLAIN QUERY PLAN), and test honesty all verified live. One surviving minor (R4-F1): the NOCASE column collation lets LIKE admit case-variant aliases (`Target:foo`) that the case-sensitive NOT GLOB never rejects — a hand-edited/model-written frontmatter alias could land in the mirror set. Fixed verbatim with the prosecutor's prescribed one-liner (`AND alias GLOB 'target:*'` case-sensitive prefix twins) + test seeds `Target:foo`/`TARGET:34`; per the slice-1 precedent, the re-review round for a one-line prescribed fix applied verbatim is waived and recorded here instead.
- Slice 4 review: converged in 4 rounds (1 blocker + 3 majors round 1, minors-only round 2+, zero guard-test edits across all rounds). Codex unavailable all rounds (CLI not installed on this machine); panel = review-prosecutor + style-guardian, with pr-review-toolkit voices unavailable in this environment.

### 2026-07-17 (final validation, Sections 3–6 — autonomous run on the work machine)
- Sections 3–6 of `memory-final-validation-task.md` executed autonomously against live `whitebit` (results in `memory-final-validation-report.md`). Owner-action drills (👎 feedback, situation dismiss/convert, Discuss "remember this" turns, preference-belief accumulation) were **deliberately NOT simulated** — fabricating owner actions would mint owner-rank/owner-action evidence from non-owner input, the exact class MEM-09/15 exist to prevent; they are queued for the owner (list at the end of the report).
- **Migration-line crossing found and hand-repaired:** the work-machine DB's goose version 16 was burned on 2026-07-14 by a transcriber-branch binary, so this branch's `00016_gmail_source` would silently never apply (gmail tables absent while goose reads v16 as done). The Up SQL was applied by hand (schema verified byte-equivalent); 00020–22 auto-applied. NEEDS-OWNER-REVIEW: before merging to main, decide whether dev DBs with crossed lines get a schema-presence assertion or the gmail migration is renumbered.
- **NEW FINDING (major, pre-existing — phases 0–3 interplay, not phase-5):** the DASH composer can attach one inbox signal to two situations (103 + 106 share `C04KTHBT9FU 1784116639.229919`); episode dedupe (provenance-overlap-keyed) then merged their two situation-mirror episodes into ONE node (`ep_01KXKD1HZ3…`, aliases `situation:103` + `situation:106`), and the situations-ingest refresh now ping-pongs that node's content between the two situations' stories EVERY consolidate — permanent one-commit-per-run churn, one story erasing the other, and the loser's provenance ref dropped/re-added each flip. Recommended fix (not applied in the validation session): exclude nodes carrying a `situation:` alias from dedupe merge candidates, or key situation-mirror identity on the alias rather than ref overlap.
- Validation-session judgment call: `memory.semantic.enabled` was toggled OFF for the mechanical drills (actions/calendar/mirrors — they are semantic-independent by design) to keep attribution clean and AI spend near zero, and restored to the pre-session value afterward.
- **NEW FINDING (major, slice-1 defect — M7 in the report):** `ListInteractionSituations` keys the verdict re-scan on `status IN ('converted','dismissed','done')` and ignores `resolved_reason`, so the inbox pipeline's **auto-resolves** (`signals_resolved`) fold as owner verdicts — false `owner resolved` `## Outcome` bullets + false owner-engagement, and (with 5D on) preference beliefs about the owner would form from system behavior. Live-caught when a daemon auto-resolve folded mid-session; every terminal situation in the 14-day window turned out to be `signals_resolved` (zero genuine owner verdicts exist in this workspace yet). Contamination reverted (vault git reverts + `memory_engagement` cleared). Fix before enabling `memory.sources.actions`: owner-authored-reason filter + a guard test seeding a `signals_resolved` situation that must not fold.
- Mid-session environmental note: the Desktop app auto-respawned the daemon at 11:06 (its bundled binary), resuming sync/inbox cycles concurrently — byte-identity proofs for MEM-05/14 all predate 11:06 and stay valid; later table diffs are daemon writes, cross-checked by the memory-side write grep + guard tests. Config was restored byte-exact from the session backup after the drills.

### 2026-07-18 (owner drill, Section 3b/6 completion pass)
- Owner performed the deferred Desktop actions: 👍 on situations 99/42/49/136 (+ a 👎→👍 flip on 136), Done on situation 108 (`user_done`). No conversion, no Discuss drills yet.
- **NEW FINDING (major, integration gap — M8 in the report):** the dashboard's situation-level thumbs (redesigned in `bda8032`, 2026-07-17 15:02 — AFTER slice 1 shipped) persist to `feedback(entity_type='situation')` on both the Swift fast path and the Go CLI path; `inbox_feedback` — the only table the slice-1 interaction ingest reads (floor `memory_last_interaction_id`) — is now written only by per-item paths. The owner's primary rating gesture therefore never reaches memory's engagement/`act:` machinery: the drill's five ratings produced zero `inbox_feedback` rows. Fix direction (deferred, NEEDS-OWNER-REVIEW): second floor-driven source over `feedback('situation')` in `action_ingest` (the situation→subjects mapping already exists via the verdict path), or move the ingest wholesale. Note the two tables also have different owner-gesture semantics (situation-level vs signal-level) that 5D preference beliefs would want to distinguish.
- Side observation (inbox domain, not memory): the 👎→👍 flip on situation 136 left the `source_mute` learned rules minted by the 👎 in place — `SituationQueries.recordFeedback` acts only on -1 and the follow-up 👍 does not retract. Logged for the inbox backlog, no memory implication.
- **M8 fixed same-day (`2d615ba`) and live-verified:** additive second floor-driven source over `feedback(entity_type='situation')` (migration 00026, `act:feedback:<id>`, whitelist extended). The drill's 6 ratings folded on the next consolidate (`interactions: 6 folded (24 engagement bumps)`; situation 136's mirror faithfully carries BOTH flip bullets — `owner marked useful` + `owner dismissed` — engagement counts each rating like inbox_feedback rows); floors independent (`memory_last_situation_feedback_id`=15, `memory_last_interaction_id` untouched at 0); re-run idempotent (0 folded).
- **M2 fixed (`5c83fdb`) + live-repaired:** `DedupeEpisodes` now excludes situation-mirror nodes as winner AND loser (`SituationMirrorNodeIDs`, numeric-anchored like the R3/R4 mirror-alias discipline; `TestDedupeNeverMergesSituationMirrors` + `TestDedupeOrdinaryEpisodeNeverMergesIntoMirror`). The live merged node was split by a curator vault edit (alias `situation:106` removed, commit `15d202c`); the next ingest recreated 106's own mirror (`ep_01KXSGWRD3…`) — and the re-render revealed the alias/story pairing had actually settled correctly (103 = the Kozhushko story, matching its DB title). The per-run `memory(ingest): 1 updated` churn commit is gone.
- Operational note: the Desktop app bundle (daemon) was rebuilt by the owner at 01:42 with M7 but predates M8/M2 — needs one more `make app-dev` (or CI build) to pick them up; harmless meanwhile (the old binary simply doesn't fold situation feedback and may re-merge... no — dedupe runs only with semantic on in the daemon too, and a re-merge of the repaired pair IS possible until the bundle is rebuilt. Mitigation: none needed short-term — the next dedupe candidate scan groups by first-ref channel; 103's mirror and 106's new mirror still share the C04KTHBT9FU ref, so the OLD binary could re-merge them on its next semantic pass. Rebuild the bundle promptly.
